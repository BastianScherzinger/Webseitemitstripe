---
bereich: status
titel: Stand der Seite
stand: 2026-09-18
status: teilweise
fortschritt: 70
zusammenfassung: 18.09.2026, Zweig recht/2026-09-18-marke-im-aufbau (Verkaufsschalter VERKAUF_AKTIV, Vorgabe aus; Besucherzählung ohne Cookie, IP-Adresse und ip-api.com) nach main zusammengeführt. 18.09.2026, Paket 313 (Zweig sofort/2026-09-18-kv14-und-1-weitere, ed835a9 vor main = origin/main = d2194e1, nicht gemergt): Mails gehen ohne Umgebungsvariable von noreply@luviq-alsfeld.com statt von der nicht existierenden Domain luviq-shop.de aus (MW22) — Brevo signiert erst, wenn die Domain im Brevo-Konto bestätigt ist; KV14 wartet auf eine Telefonnummer der Betreiberin, RE03 ist unklar. Paket 306 ist seit dem Merge d2194e1 auf main und origin/main. Paket 306 (Zweig sofort/2026-09-18-pj06-und-2-weitere, 156176e, dd339b1 und 0b94d8e): Fehler beim Anlegen einer Bestellung, beim PayPal-Abschluss, bei der Registrierung und an fünf Stellen des Admin-Panels stehen jetzt im Log, statt spurlos zu verschwinden (PJ06); drei Dateien sind ohne Verhaltensänderung für das Code-Audit aufgeräumt (PJ07); jede Seite nennt Suchmaschinen und Sprachassistenten Titel und Beschreibung als vorlesbare Teile (speakable), der Antwortsatz der Produktseiten ist ein eigener Vorlagenteil, und IndexNow meldet geänderte und verkaufte Stücke an Bing — aus, bis INDEXNOW_KEY in Railway gesetzt ist (PJ13, anders eingebaut). Laut Commit 314 Tests grün, 313 Testfunktionen gezählt. Offen: Merge, INDEXNOW_KEY, und der beim Bau gefundene leere land-Wert bei der Registrierung. 18.09.2026, Paket 248 (Zweig sofort/2026-09-18-re15-und-2-weitere, d58a96c, 329dbc4 und 2f51192 vor main = 8114dd9, das seinerseits einen Doku-Commit vor origin/main = 0444272 liegt, nicht gemergt): die Google-Karte auf Startseite und Gästebuch lädt nicht mehr von selbst — an ihrer Stelle steht ein Platzhalter „Karte laden" mit dem Hinweis auf die Übertragung, und erst der Klick holt die Karte von Google (RE17); vorher ging die IP-Adresse jedes Besuchers ungefragt dorthin. Der Platzhalter entsteht im Skript von base.html und nicht in der Vorlage, damit der eingefrorene Aufbau unverändert bleibt; ohne JavaScript bleibt der Kartenrahmen leer. Der Absendeknopf des Bestellformulars heisst jetzt „Zahlungspflichtig bestellen" statt „Continue to Payment →" — genau die Beschriftung, die die AGB seit jeher nennen (RE21, nur Text). Die Datenschutzerklärung beschreibt den Platzhalter statt der automatischen Einbettung, seiten_stand.py führt datenschutz deshalb mit dem 18.09.2026. Ein Cookie-Banner wäre für diese Seite falsch und steht als begründete Ausnahme im Bewertungsblock (RE15): kein eigenes Cookie, kein Gerätespeicher, kein Analysedienst, und seit RE17 lädt nichts Einwilligungspflichtiges ohne Klick. Laut beiden Code-Commits manage.py check grün und 290/290 Tests grün; im Browser ungeprüft. Von den eigenen Punkten ist EIG14 (Karte lädt vor jeder Einwilligung) damit erledigt und EIG64 zur Hälfte — Warenkorb, Checkout und Zahlung verlinken weiter weder AGB noch Widerruf. 17.09.2026, Paket 243 (Zweig sofort/2026-09-17-kv05-und-2-weitere, b736994, 06503a7 und 9cd641f vor main = origin/main = 911c26a, nicht gemergt): das Kontaktformular nennt den Datenschutz jetzt im Formular selbst — was gespeichert wird, dass die Antwort über Brevo hinausgeht, wo es Auskunft und Löschung gibt und unter welcher Adresse die Datenschutzerklärung steht (KV05, als Fliesstext ohne eigenes Element, weil die Designwache Elemente zählt); das Fallenfeld des Spamschutzes trägt aria-hidden, seine eigene Verschiebung aus dem Bild, tabindex=-1 und autocomplete=off jetzt selbst (KV06). KV09 (Vertrauenssignale) ist nicht möglich: weder Gründungsjahr noch Zertifikat noch eine belegte Bewertungszahl stehen im Projekt. 287 Tests grün. Paket 238 (Zweig sofort/2026-09-17-bf24-und-2-weitere, 8014429 und 22fea97 vor main = origin/main = f489e76, nicht gemergt): das Kontaktformular sagt Fehler jetzt selbst an — die Meldung steht im Formular (div id=kontakt-fehler mit role=alert) und hängt über aria-describedby an jedem der vier Pflichtfelder, statt wie bisher im Meldungsbereich ganz oben zu stehen (BF24 nachgebessert); dazu ein RSS-Feed unter /feed/ mit den freigegebenen Wissensbeiträgen, verlinkt im Kopf jeder Seite und in llms.txt, damit Aggregatoren und Antwortmaschinen neue Inhalte finden, ohne die Seite abzulaufen (GE32). 285 Testfunktionen gezählt, laut Bausitzung grün; im Bildschirmleser und in einem Feedleser ungeprüft. IS23 (zwei Produkte mit demselben Hauptbegriff) ist nicht im Code lösbar: Titel und Slug entstehen aus dem Produktnamen — eindeutige Namen oder seo_titel kann nur die Betreiberin im Shop-Admin vergeben. Paket 227 ist seit dem Merge f489e76 auf main und origin/main. Paket 227 (Zweig sofort/2026-09-17-pj05-und-2-weitere, a708c7f und a8190b8, seit f489e76 auf main): ein Cache-Fehler der Newsletter-Tagessperre wird protokolliert statt verschluckt (PJ05), und Fehlermeldungen werden Bildschirmlesern angesagt — role="alert" an Fehlermeldungen, aria-live am Kontaktformular (BF24); 278 Testfunktionen, laut Bausitzung grün. PF31 (Skripte selbst ausliefern) ist nicht möglich, die vier Bibliotheksdateien fehlen im Projekt. Paket 223 (PJ05-Vermerk an kontakt(), GE43 Gesetzesquellen im Wissensbereich) ist seit f8643f0 auf main. Ebenfalls 17.09.2026: keine automatischen Mails mehr an eingetippte Adressen (`a437803`). Gesamtstand 76,5 „Solide“ (04.09.2026). Neu im Zweig sofort/2026-09-17-bf29-und-2-weitere (Paket 219, zwei Commits vor main = origin/main = 8717afe, nicht gemergt): Pillow steht auf 12.3.0 statt 11.3.0 (SI40, installiert und getestet noch nirgends), und das Kontaktformular speichert jede angenommene Anfrage vor dem Mailversand im Modell KontaktAnfrage, sodass ein Mailausfall keine Nachricht mehr verschluckt (MW18); 270 Testfunktionen, laut Bausitzung grün. Die Datenschutzerklärung nennt diese Speicherung noch nicht. BF29 (Kontrast im Seitenrahmen) ist benannt, braucht aber eine Farbentscheidung der Betreiberin. Paket 216 ist seit dem Merge 8717afe auf main: das Kontaktformular sperrt beim Absenden seinen Knopf und verschickt eine wortgleiche zweite Anfrage nicht noch einmal (FO03), seine vier Pflichtfelder tragen einen Stern (FO04) und sind in der Länge wie auf dem Server begrenzt (FO07); die Newsletter-Anmeldung sperrt ihren Knopf ebenfalls; 264 Testfunktionen, laut Bausitzung grün, im Browser ungeprüft. Paket 208 (FO06, FO08, FO09: serverseitige Prüfung, Drosselung je IP, generate_lead im dataLayer) ist seit d978a89 auf main, der Spamschutz mit Fallenfeld, Zeitstempel und Punkteschwelle seit d90067a, der Zweig mit pruefe_mail und GE15 ist ebenfalls gemergt. Verbesserungslauf 4, SU04 und GE23/GE25/IS19 liegen auf origin/main; drei belegte Wissensbeiträge, Antwortabsätze mit belegten Zahlen, keine dünne Seite mehr. PJ05 (kritische Audit-Funde abgearbeitet oder begründet), SI08 (CSP blockiert) und PJ11 (Paketfassungen samt Lockfile) sind seit dem Merge 0c18ea7, SI09 (Nonce statt 'unsafe-inline' im script-src, im Browser ungeprüft) seit 4ec540b, IS18 seit 1a5f36b, KV07 samt der VL01-Ausnahme seit c522ff9, VL21 (Produktkarte als zwei Bausteine, gerendertes HTML unverändert) seit 0d5500b und SI17/PJ01 (integrity an den vier Fremdskripten, Prüfbefehl pruefe_links) seit 43f25c3 auf main. Mit dem Zweig sofort/2026-09-12-mw15-und-2-weitere kam der dritte Prüfbefehl pruefe_mail (MW15: Einstellungen und Anmeldung beider Mailwege) auf main, und die sechs Wissensbeiträge tragen einen Article-Knoten mit Autorin und belegten Daten, die Übersicht eine ItemList (GE15). GE20 (Öffnungszeiten) ist nicht anwendbar: es gibt kein Ladengeschäft. SU08 ist nicht möglich — die vier einseitigen Bereiche brauchen Inhalte der Betreiberin. GE11 wartet auf Profiladressen der Betreiberin, RE07 auf Schriftdateien und einen Lauf mit Netzzugang. TS11 wartet weiter auf CANONICAL_HOST. Die drei ersten Beiträge und die Produktbeschreibungen warten auf die Betreiberin.
offen: 3
quellen: CLAUDE.md, DOCUMENTATION.md, LOGBUCH.md
---

# Stand — Luviq Universe

*Woran sich der Fortschritt bemisst: am Gesamtstand der letzten Messung. Die Bezugsgröße jedes Bereichs steht in der jeweiligen Datei unter der Überschrift; sie ist bei allen sechs betreuten Seiten dieselbe.*

## Steckbrief

| | |
|---|---|
| **Kundin** | Luisa Brehler (Anrede: Frau Brehler) |
| **Sitz** | Grünberger Str. 16, 36304 Alsfeld (Hessen) · brehlerluisa@gmail.com · kein Ladengeschäft, Verkauf nur online |
| **Art** | echter Onlineshop mit Bestellungen, Kundenkonten und Zahlung — kein Prospekt |
| **Angebot** | handbemalte Second-Hand-/Vintage-Kleidung, jedes Stück 1-of-1; Versand deutschlandweit, in der Regel 1–2 Werktage |
| **Bezahlung** | **PayPal** oder Vorab-Überweisung; Endpreise nach § 19 UStG (Kleinunternehmerin) — **kein Stripe** |
| **Domain** | `https://www.luviq-alsfeld.com` (200) · Alias `https://luviq-alsfeld.com` (**antwortet ebenfalls 200, keine Weiterleitung** — Stand 02.09.2026) · Railway `luviq-luisa-shop.up.railway.app` (200) |
| **Projektordner** | `C:\Users\basti\Desktop\webseiten buisnes\WebseiteMAIN` |
| **GitHub** | `BastianScherzinger/Webseitemitstripe` · Hauptzweig `main` |
| **Railway** | Projekt `webseiten` → Dienst `Luviq-Luisa` (Umgebung `shop`) |
| **Technik** | Django (≥ 5.0, < 6.1; im Zweig vom 11.09.2026 `==5.2.17` mit `requirements.lock`), Docker (`python:3.11-slim`), PostgreSQL, Cloudinary (Bilder), Brevo (Mail), django-axes, Tailwind-CLI (kein npm im Repo) |
| **Live-Stand** | `main` @ `2a17edd` (15.07.2026); letzte Railway-Auslieferung 18.08.2026 (`645842b`, „docs: README zum Luviq-Universe-Onlineshop", SUCCESS) |
| **Ordner-Stand** | Zweig `sofort/2026-09-17-kv05-und-2-weitere` @ `9cd641f` (17.09.2026), drei Commits vor `main` = `origin/main` = `911c26a` (`b736994` KV05, `06503a7` KV06, `9cd641f` `seiten_stand.py` für `kontakt`), nicht gemergt, nicht gepusht (lokale Referenz, geprüft 17.09.2026); `911c26a` ist der Merge von Paket 238 (`8014429`, `22fea97`). Ältere Angabe: Zweig `sofort/2026-09-17-bf24-und-2-weitere` @ `22fea97`, damals zwei Commits vor `main` = `origin/main` = `f489e76` (`8014429` BF24, `22fea97` GE32), inzwischen mit `911c26a` gemergt; `f489e76` war der Merge von Paket 227 (`a708c7f`, `a8190b8`). Ältere Angabe: Zweig `sofort/2026-09-17-pj05-und-2-weitere` @ `a8190b8`, damals zwei Commits vor `main` = `origin/main` = `f8643f0` (`a708c7f` PJ05, `a8190b8` BF24); `f8643f0` war der Merge von Paket 223 (`6b618e4`, `5928144`). Ältere Angabe: Zweig `sofort/2026-09-17-bf29-und-2-weitere` @ `0eb7589`, inzwischen mit `dd804dc` gemergt — damals zwei Commits vor `main` = `origin/main` = `8717afe` (`2cfa8ee` SI40 — Pillow 12.3.0; `0eb7589` MW18 — `KontaktAnfrage`), nicht gemergt, nicht gepusht (lokale Referenz, geprüft 17.09.2026). `sofort/2026-09-17-fo03-und-2-weitere` (Paket 216: `3e0a59f` FO03, `370fce7` FO04, `641153b` FO07) ist mit `8717afe` gemergt. `sofort/2026-09-16-fo06-und-2-weitere` ist mit `d978a89` gemergt, `sofort/2026-09-12-mw15-und-2-weitere` ist in `main` enthalten (`git branch --merged main`). Ältere Angaben dieser Zeile: `sofort/2026-09-12-si17-und-2-weitere` ist mit `43f25c3`, `sofort/2026-09-12-vl21-und-2-weitere` mit `0d5500b`, `sofort/2026-09-11-kv07-und-2-weitere` mit `c522ff9`, `sofort/2026-09-11-is18` mit `1a5f36b`, `sofort/2026-09-11-si09` mit `4ec540b`, `sofort/2026-09-11-pj05-und-2-weitere` mit `0c18ea7` in `main` gemergt; `main` = `69c1f78`, `origin/main` = `43f25c3` — **`main` ist einen Doku-Commit voraus, der Push steht aus** (lokale Referenz, geprüft 12.09.2026). `sofort/2026-09-12-re07-und-2-weitere` (Paket 188) ist nicht gemergt, unterscheidet sich von `main` aber nur in `LOGBUCH.md` und `doku/`. `cockpit/2026-09-01-verbesserung-4` (`511ffe5`) ist in `origin/main` enthalten — die Zeile „Live-Stand" darüber ist damit überholt (siehe eigener Punkt `EIG11` in [80-AUFGABEN.md](80-AUFGABEN.md)) |
| **Zertifikat** | Let's Encrypt, TLS 1.3, gültig bis 30.11.2026 (Messung 02.09.2026: 89 Resttage); Alias eigenes Zertifikat bis 30.11.2026 |
| **Verfügbarkeit** | 24 h: 100 % (1.670 Messungen, Ø 1.029 ms) · 7 Tage: 99,92 % (3.935 Messungen, Ø 1.099 ms) — Messung 02.09.2026 |

## Ampel je Bereich

Aus den Köpfen der zehn Bereichsdateien (Stand 02.09.–17.09.2026).

| Bereich | Status | Fortschritt | Zusammenfassung | Datei |
|---|---|---:|---|---|
| Wegweiser | vollständig | 100 | Elf Dateien nach Doku-Standard; die Original-Doku im Projektstamm bleibt die Detailquelle. | [README.md](README.md) |
| Technik | teilweise | 74 | Paket 248 (18.09.2026, Zweig `sofort/2026-09-18-re15-und-2-weitere`, nicht gemergt): die Google-Karte auf `/` und `/gaestebuch/` trägt ihre Adresse in `data-src` statt in `src`, das Skript am Körperende von `base.html` legt darüber den Knopf „Karte laden" und setzt `src` erst im Klick (`RE17`) — vorher geht keine Anfrage an `maps.google.com`, ohne JavaScript bleibt der Rahmen leer. Fünftes Ersatzattribut neben `data-bestaetigen`, `data-bei-fehler-ausblenden`, `data-auto-absenden` und `data-schrift-nachladen`; neue Falle: `maps.google.com` muss in `CSP_QUELLEN['frame-src']` bleiben, obwohl keine Vorlage die Adresse mehr als `src` führt (eigener Test). Dazu der deutsche Bestellknopf (`RE21`, nur Text) und die Datenschutzerklärung mit Platzhaltersatz; laut Commits `manage.py check` grün, 290/290 Tests grün, im Browser ungeprüft. Paket 243 (nicht gemergt): das Kontaktformular nennt den Datenschutz im Formular selbst (`KV05`, als Fliesstext ohne eigenes Element, weil die Designwache Elemente zählt), und das Fallenfeld des Spamschutzes trägt `aria-hidden`, seine eigene Verschiebung aus dem Bild, `tabindex="-1"` und `autocomplete="off"` jetzt am Feld statt nur am umgebenden Kasten (`KV06`); 287 Tests grün (343 s), `test_aufbau` unverändert. Neue Falle für Vorlagen: ein mehrzeiliger `{# … #}`-Kommentar bleibt im ausgelieferten HTML stehen, und seine Beispiel-Tags zählt die Designwache als echte Elemente — mehrzeilig nur mit `{% comment %}`. Paket 238 (seit `911c26a` auf `main`): die Fehlermeldung des Kontaktformulars kommt als Kontextwert `fehler` in die Vorlage statt über `messages` und steht mit `role="alert"` im Formular, an jedem Pflichtfeld per `aria-describedby` (`BF24`); neue klassenbasierte View `WissenFeed` mit Route `/feed/` (`GE32`, wie jede View in `views/__init__.py` re-exportiert); 285 Testfunktionen. Paket 227 (seit `f489e76` auf `main`): Cache-Fehler der Newsletter-Tagessperre im Log statt verschluckt (`PJ05`), `role="alert"`/`aria-live` für Formularfehler (`BF24`); `PF31` nicht möglich. Stack läuft stabil. Im Zweig `sofort/2026-09-17-bf29-und-2-weitere` (Paket 219, seit `dd804dc` gemergt) `pillow==12.3.0` (`SI40`, noch nirgends installiert) und das Modell `KontaktAnfrage` (`MW18`, Migration `0019`): Kontaktanfragen werden vor dem Versand gespeichert und stehen im Django-Admin; 270 Testfunktionen. Paket 216 ist seit `8717afe` auf `main`: dort sperrt das Kontaktformular beim Absenden seinen Knopf (`data-einmal-absenden`, Skript in `base.html`), `doppelt_abgeschickt()` führt wortgleiche Anfragen binnen zwei Minuten ohne zweite Mail zusammen (je Prozess), dazu Pflichtfeld-Stern und `maxlength`; 264 Testfunktionen. Auf `main` seit `d978a89` serverseitige Prüfung und Drosselung je IP, seit `d90067a` der Spamschutz (`shop1/spamschutz.py`), und `pruefe_mail` ist gemergt. Ältere Angaben: seit den Merges `0c18ea7`, `4ec540b`, `1a5f36b`, `c522ff9`, `0d5500b` und `43f25c3` liegen auf main die scharf gestellte CSP, die Nonce im `script-src` statt `'unsafe-inline'`, die festgenagelten Paketfassungen samt `requirements.lock` (Django 5.2.17), die abgearbeiteten Audit-Funde, die Danke-Seite `/kontakt/danke/`, die Produktkarte als zwei Bausteine unter `shop1/templates/shop1/teile/` (Falle: `load` wird an ein `include` nicht vererbt), `integrity`/`crossorigin` an den vier Fremdskripten und der zweite Prüfbefehl `pruefe_links`; `main` = `69c1f78`, ein Doku-Commit vor `origin/main`. Im Zweig `sofort/2026-09-12-mw15-und-2-weitere` (drei Commits, nicht gemergt) steht als dritter Prüfbefehl `pruefe_mail`: Einstellungen beider Mailwege, Anmeldung an Brevo-API und SMTP-Relay, Testmail mit `--an`, Geheimnisse nur als „gesetzt (n Zeichen)" — wie `pruefe_links` bewusst nicht an `start.sh` angeschlossen und gegen die echten Zugangsdaten noch nie gelaufen. Offen bleiben der Merge, Bezahlseite und Ersatzskripte mit Browserkonsole, ein echter `pruefe_mail`-Lauf, die zwei Chart.js-Einbindungen des Admin-Panels ohne `integrity`, `'unsafe-eval'` für Alpine.js, der erste CI-Lauf mit 5.2.17, `CANONICAL_HOST` in Railway, `runtime.txt`/`railway.json` und die Permissions-Policy. | [10-TECHNIK.md](10-TECHNIK.md) |
| Design | teilweise | 77 | Dunkelbraun-Gold-Linie mit Glas-Karten steht und ist durch die Designwache eingefroren. Paket 248 (nicht gemergt) ändert zwei sichtbare Dinge, ohne den eingefrorenen Aufbau zu berühren: an der Stelle der Google-Karte steht bis zum Klick ein dunkler Platzhalter „Karte laden" (`RE17`; er entsteht erst im Skript, trägt Inline-Stile statt Klassen der Linie und ist im Browser ungeprüft — ohne JavaScript bleibt der Rahmen leer), und der Bestellknopf heisst „Zahlungspflichtig bestellen" statt „Continue to Payment →" (`RE21`, nur Text; eine englische Restbeschriftung weniger). `aufbau_referenz.json` nicht angefasst. Paket 243 (nicht gemergt) setzt einen sichtbaren Datenschutzhinweis ins Kontaktformular — reiner Fliesstext ohne eigenes Element und ohne eigene Klasse, damit der eingefrorene Aufbau unverändert bleibt; er erbt dadurch Schrift und Farbe des Formularrahmens, im Browser ungeprüft. Seit `BF24` (Paket 227, seit `f489e76` auf `main`) werden Meldungen Bildschirmlesern angesagt — nur Attribute, Aussehen unverändert; mit der Nachbesserung aus Paket 238 (seit `911c26a` auf `main`) steht die Fehlermeldung des Kontaktformulars im Formular selbst, in vorhandenen Klassen und nur im Fehlerfall gerendert, damit der sichtbare Aufbau unverändert bleibt (`aufbau_referenz.json` nicht angefasst); die Produktkarte liegt seit dem 12.09.2026 als Baustein unter `shop1/templates/shop1/teile/`, ohne eine Änderung am gerenderten HTML. Seit Paket 219 benannt: zu schwacher Kontrast im Seitenrahmen (`BF29` — Fusslinks, Copyright-Zeile, „Registrieren" weiss auf Gold), nicht behoben, weil es eine Farbentscheidung der Betreiberin braucht. Offen sind ausserdem Google-Schriften von fremdem Server (RE07, am 12.09.2026 erneut als nicht möglich beendet: keine WOFF2 im Projekt, kein Netzzugang im Lauf), englische Restbeschriftungen und seit IS18 die Satzart des langen Versalien-Absatzes auf `/produkte/` (braucht Freigabe). | [20-DESIGN.md](20-DESIGN.md) |
| Inhalte | teilweise | 40 | Paket 248 (nicht gemergt) ändert drei Textstellen: der Bestellknopf heisst „Zahlungspflichtig bestellen" und deckt sich damit mit den AGB, die diese Beschriftung längst nennen (`RE21`); der Abschnitt „Hosting & Infrastruktur" der Datenschutzerklärung beschreibt statt der automatischen Karte den Platzhalter und den Klick (`RE17`, `seiten_stand.py` für `datenschutz` auf den 18.09.2026 nachgezogen); der Platzhaltertext „Karte laden" selbst steht in keiner Vorlage, sondern entsteht im Skript und zählt in keiner Wortzahl mit. Paket 243 (nicht gemergt) setzt neuen sichtbaren Text auf `/kontakt/`: der Datenschutzhinweis steht jetzt zwischen Nachrichtenfeld und Absendeknopf und nennt, was gespeichert wird, den Weg über Brevo, die Stelle für Auskunft und Löschung und die Adresse der Datenschutzerklärung (`KV05`) — jede Angabe aus `legal/datenschutz.html`, ohne Rechtsgrundlage und ohne Frist, weil beides im Projekt nicht steht. `seiten_stand.py` führt `kontakt` seitdem mit dem 17.09.2026. `KV09` (Vertrauenssignale) ist im selben Paket nicht möglich: Gründungsjahr, Zertifikate und eine belegte Bewertungszahl gibt es nirgends. Live 14 URLs mit 1.557 Eigenwörtern (85 % dünne Seiten); main füllt neun Seiten mit Auskunft, bringt sechs Wissensbeiträge (drei belegt und indexiert, drei warten auf die Freigabe) und hält jede indexierbare Seite über 200 Wörtern. Seit IS18 (Merge `1a5f36b` auf main) erreichen Startseite, Produktübersicht und Wissensübersicht laut Commit mit belegtem Fliesstext die Zielgrösse ihrer Seitenart — nur die Produktseiten brauchen weiter eine eigene Beschreibung je Einzelstück. Der Zweig `sofort/2026-09-11-kv07-und-2-weitere` (nicht gemergt) bringt die Danke-Seite `/kontakt/danke/` (KV07): noindex, Text nur mit Angaben, die schon auf `kontakt.html` stehen. `SU08` (jeder Themenbereich mit mehr als einer Seite) ist am 12.09.2026 geprüft und als nicht möglich beendet: `/produkte/` ist die Übersicht zu `/produkt/<slug>/`, und `/gaestebuch/`, `/ueber_uns/`, `/liefergebiet/` bräuchten Inhalte, die es im Projekt nicht gibt. Seit GE15 (12.09.2026, Zweig) führt `WISSEN_BEITRAEGE` je Beitrag zusätzlich das Feld `veroeffentlicht` — ein neuer Beitrag braucht auch diesen Eintrag; am sichtbaren Text ändert sich nichts. | [30-INHALTE.md](30-INHALTE.md) |
| SEO / GEO | teilweise | 74 | Neu im Zweig `sofort/2026-09-18-re15-und-2-weitere` (Paket 248, nicht gemergt): `seiten_stand.py` führt `datenschutz` mit dem 18.09.2026 statt dem 01.09.2026, weil `RE17` den Abschnitt „Hosting & Infrastruktur" umgeschrieben hat — `lastmod` und `dateModified` von `/datenschutz/` stimmen damit wieder zum sichtbaren Text. Sonst unberührt: die Karte ist eine Einbettung, kein Rankingfaktor, und der umbenannte Bestellknopf steht auf einer nicht indexierten Seite. Neu im Zweig `sofort/2026-09-17-kv05-und-2-weitere` (Paket 243, nicht gemergt): `seiten_stand.py` führt `kontakt` mit dem 17.09.2026 statt dem 01.09.2026 — `lastmod` in der Sitemap und `dateModified` des `WebPage`-Knotens von `/kontakt/` stimmen damit wieder zum sichtbaren Text (Datenschutzhinweis aus `KV05`); das Register wird von Hand geführt, kein Test erzwingt es. Seit `911c26a` auf `main` (Paket 238): ein RSS-Feed unter `/feed/` führt die freigegebenen Wissensbeiträge mit Titel, Kurztext und Datum aus dem Register — genau die Menge aus Sitemap und `llms.txt`, verlinkt als `rel="alternate"` im Kopf jeder Seite und als Zeile `Feed:` in `llms.txt` (`GE32`). `IS23` (Kannibalisierung durch „custom print") steht seitdem als beim Kunden im Bewertungsblock: Titel und Slug entstehen aus dem Produktnamen. SEO-Technik live solide (92), Inhalt 73, GEO 76; llms.txt, KI-Crawler-Regeln, WebPage/Person/Breadcrumb-Knoten, 301 auf www, drei indexierte Wissensseiten und seit dem 08.09.2026 GE23/GE25/IS19 (Antwortabsatz, belegte Zahlen, keine dünne Seite mehr) liegen fertig im Code, nicht nachgemessen. IS18 (seit dem Merge `1a5f36b` auf main) bringt Startseite, Produktübersicht und Wissensübersicht laut Commit auf den Umfang ihrer Seitenart und zieht deren lastmod auf den 11.09.2026; die Produktseiten bleiben darunter. Die Danke-Seite `/kontakt/danke/` (KV07) ist seit dem Merge `c522ff9` auf main, noindex und weder in Sitemap noch in llms.txt. GE11 (`sameAs`) ist am 11.09. und am 12.09.2026 nicht möglich und steht seit dem 12.09.2026 als beim Kunden im Bewertungsblock, weil ausser Instagram keine Profiladresse dokumentiert ist. GE15 (12.09.2026, Zweig `sofort/2026-09-12-mw15-und-2-weitere`) gibt jedem der sechs Wissensbeiträge einen `Article`-Knoten mit Autorin `#luisa` und belegten Daten aus den Registern und der Übersicht eine `ItemList`; GE20 (Öffnungszeiten) ist als nicht anwendbar eingetragen, weil es kein Ladengeschäft gibt. | [40-SEO.md](40-SEO.md) |
| Local SEO | teilweise | 25 | Die eingebettete Google-Karte lädt seit Paket 248 (`RE17`, nicht gemergt) erst nach einem Klick; Anschrift, Bewertungsknopf und Schema bleiben, der Local-SEO-Stand ändert sich dadurch nicht. Search Console seit 03.09.2026 verbunden (Property `sc-domain:luviq-alsfeld.com` im Konto …05@gmail.com); Unternehmensprofil und Bewertungen bleiben nicht dokumentiert — der Standardwert von `GOOGLE_REVIEW_URL` ist ein Maps-Suchlink, kein Profil (`settings.py:409`, geprüft 12.09.2026). Die Platzhalterdaten auf /kontakt/ und „www.luviq.de“ im Impressum sind auf main ersetzt, ob Railway das ausgeliefert hat, ist nicht geprüft. GE11 (`sameAs`) ist am 11.09. und am 12.09.2026 nicht möglich und steht seit dem 12.09.2026 als beim Kunden im Bewertungsblock: ausser Instagram ist keine Profiladresse dokumentiert. | [50-LOCAL-SEO.md](50-LOCAL-SEO.md) |
| Ads | nicht zutreffend | — | Es gibt keine Google-Ads-Kampagne und kein Konto; Voraussetzungen für Shopping-/Suchanzeigen sind benannt. | [60-ADS.md](60-ADS.md) |
| Performance | teilweise | 70 | PageSpeed mobil 68 (Start) bis 89, LCP mobil 5,3 s auf der Startseite und 23 s auf /produkte/; der Zweig bringt WebP, GZip, Cache und gthread, live ist davon nichts. `PF31` (Skripte selbst ausliefern) ist am 17.09.2026 nicht möglich — die vier Bibliotheksdateien fehlen im Projekt. | [70-PERFORMANCE.md](70-PERFORMANCE.md) |
| Aufgaben | teilweise | 50 | Paket 248 (18.09.2026, `d58a96c`, `329dbc4`, `2f51192`, nicht gemergt): `RE17` eingebaut (Karte erst nach Klick), `RE21` eingebaut (Knopf „Zahlungspflichtig bestellen"), `RE15` als nicht anwendbar im Bewertungsblock (ohne eigenes Cookie, ohne Gerätespeicher, ohne Analysedienst und ohne selbstladende Einbettung gibt es nichts, worüber ein Banner einwilligen liesse). Damit sind die eigenen Punkte `EIG14` erledigt und `EIG64` zur Hälfte — die AGB- und Widerrufslinks im Bestellweg fehlen weiter. Offen sind Merge und Push sowie der Blick auf Platzhalter, Knopf und den Ablauf ohne JavaScript im Browser. Paket 243 (17.09.2026, `b736994`, `06503a7`, `9cd641f`, seit dem Merge `0444272` auf `main` und `origin/main`): `KV05` anders eingebaut (Datenschutzhinweis im Kontaktformular, als Fliesstext ohne eigenes Element), `KV06` eingebaut (das Fallenfeld ist an sich selbst versteckt), `KV09` als beim Kunden im Bewertungsblock (Gründungsjahr, Zertifikate und eine echte Bewertungszahl kann nur die Betreiberin liefern, „Beim Kunden" Nr. 19); Merge und Push sind erledigt, offen bleibt der Blick auf `/kontakt/` im Browser. Paket 238 (17.09.2026, `8014429`, `22fea97`) ist seit `911c26a` auf `main` und `origin/main`: `BF24` nachgebessert (Meldung im Formular, an jedem Pflichtfeld), `GE32` eingebaut (RSS-Feed `/feed/`), `IS23` als beim Kunden im Bewertungsblock (Produktnamen setzt nur die Betreiberin); offen bleibt die Probe mit Bildschirmleser und Feedleser. Paket 227 (17.09.2026, `a708c7f`, `a8190b8`) ist seit `f489e76` auf `main`: `PJ05` nachgebessert, `BF24` eingebaut, `PF31` als beim Kunden im Bewertungsblock (Skriptdateien fehlen). Paket 223 ist seit `f8643f0` auf `main`. Paket 219 (17.09.2026, Zweig `sofort/2026-09-17-bf29-und-2-weitere`, `2cfa8ee`, `0eb7589`, seit `dd804dc` gemergt) bringt SI40 und MW18; offen sind Merge, erster Lauf mit Pillow 12.3.0 und die Speicherung der Anfragen in der Datenschutzerklärung. BF29 steht als beim Kunden im Bewertungsblock (Farbentscheidung). Paket 216 (`3e0a59f`, `370fce7`, `641153b`) ist seit `8717afe` auf `main`; offen sind dort Browserprüfung, `seiten_stand.py` für `kontakt` und Stern/`maxlength` an den übrigen Formularen. Paket 208 ist seit `d978a89` auf `main`, Honigtopf, Zeitfalle und Punkteschwelle seit `d90067a`. Ältere Angaben: SU04, GE23, GE25, IS19, PJ05, SI08, PJ11, SI09, IS18, KV07, VL01, VL21 und seit dem Merge `43f25c3` auch SI17 und PJ01 liegen auf main (`69c1f78`; `origin/main` steht einen Doku-Commit zurück, ob Railway ausgeliefert hat, ist nicht geprüft; SI09 und die Danke-Seite im Browser ungeprüft). Paket 200 (12.09.2026, Zweig `sofort/2026-09-12-mw15-und-2-weitere`, drei Commits `4fd7776`, `be550e3` und `bb6fee1`, nicht gemergt) bringt MW15 und GE15: den dritten Prüfbefehl `pruefe_mail` (Einstellungen und Anmeldung beider Mailwege, Testmail mit `--an`) und den `Article`-Knoten auf allen sechs Wissensbeiträgen samt `ItemList` auf der Übersicht — 243 Testfunktionen, laut Commit grün, kein Template-Aufbau berührt. GE20 im selben Paket als nicht anwendbar im Bewertungsblock: Öffnungszeiten setzen ein Ladengeschäft voraus, das es hier nicht gibt. Nächster Schritt ist der Merge dieses Zweigs samt Push, danach Nachmessung, Bezahlseite und Ersatzskripte mit offener Browserkonsole, ein Lauf von `pruefe_mail` gegen die echten Zugangsdaten und der erste CI-Lauf mit Django 5.2.17; Schriften lokal, Sprungmarke, Icon-Knöpfe, Permissions-Policy und die Freigaben der Betreiberin bleiben offen. | [80-AUFGABEN.md](80-AUFGABEN.md) |
| Notizen | vollständig | 100 | Vier Namen für ein Projekt, PayPal statt Stripe, Zweig gegenüber main, zehn Widersprüche zwischen Quellen und Live-Seite. Der Satz „kein Consent-Banner nötig" ist seit dem 18.09.2026 belegt statt behauptet — kein eigenes Cookie, kein Gerätespeicher, kein Analysedienst, und die Karte lädt erst nach einem Klick (`RE17`, `RE15`). | [90-NOTIZEN.md](90-NOTIZEN.md) |

## Messung

<!-- messung:anfang -->
**Messung vom 18.09.2026** (Webagentur Scherzinger Overview, Regelstand 2026-09-17b) — **Gesamtstand 87,0 von 100**, Reifegrad „Solide“. 331 von 372 Regeln an 18 URLs und 137 Dateien (31.698 Zeilen) geprüft.

| Bereich | Wert | Reifegrad |
|---|---:|---|
| Konversion | **72** | Brauchbar |
| Erreichbarkeit & Postfach | **75** | Solide |
| Recht & Vertrauen | **76** | Solide |
| Vorlagen-Konformität | **79** | Solide |
| Betrieb & Auslieferung | **80** | Solide |
| SEO — Inhalt | **81** | Solide |
| Substanz & Reichweite | **82** | Solide |
| GEO — KI-Sichtbarkeit | **87** | Solide |
| Barrierefreiheit | **88** | Solide |
| Code-Qualität & Projektreife | **90** | Referenz |
| Performance & Core Web Vitals | **91** | Referenz |
| SEO — Technik | **95** | Referenz |
| Sicherheit | **96** | Referenz |
| Formulare & Preisrechner | **100** | Referenz |

Keine Sperre greift.

Quelltext: 137 Dateien, **105 Befunde**, davon 0 kritisch und 39 wichtig.

Kritische Befunde:

- **Keine Beinahe-Duplikate zwischen Seiten** (`IS21`) — 10 Seitenpaare über 60 Prozent Textgleichheit, höchster Wert 100%: /produkt/custom-hoodie-mit-print/ = /produkt/custom-print-hoodie-1/ (100%), /produkt/custom-hoodie-mit-print/ = /produkt/custom-print-hoodie/ (99%), /produkt/custom-print-ho
- **axe-core findet keine schweren Verstösse** (`BF29`) — 4 von 4 Seiten mit schweren Verstössen: color-contrast (4×)
- **Die Telefonnummer ist anklickbar und steht auf jeder Seite** (`KV01`) — 0 von 17 Seiten mit tel:-Link (Startseite: nein) — ohne: /, /produkte/, /kontakt/, /datenschutz/, /agb/ … (+12)
- **Keine Google-Schriften von fremden Servern nachgeladen** (`RE07`) — 17 von 17 Seiten laden Google-Schriften von fremden Servern: fonts.googleapis.com, https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@700 · betroffen: /, /produkte/, /kontakt/ … (+14)
- **Eine eigene Sicherung außerhalb des Hosters ist frisch und geprüft** (`BT15`) — keine einzige eigene Sicherung; der letzte Versuch scheiterte: Docker läuft nicht — ohne pg_dump keine externe Sicherung
- **Die Domain nimmt überhaupt E-Mail an** (`MW01`) — Für luviq-alsfeld.com ist **kein MX-Eintrag** gesetzt. Eine Mail an eine Adresse dieser Domain kommt nirgends an.
- **Ein SPF-Eintrag sagt, wer im Namen der Domain schreiben darf** (`MW04`) — Kein SPF-Eintrag auf luviq-alsfeld.com. Jede Mail im Namen dieser Domain ist für einen Empfänger ununterscheidbar von einer gefälschten.
- **Kein nennenswerter Anteil dünner Seiten** (`IS19`) — 6% der Seiten sind dünn (1 von 17): /gaestebuch/ (130 W)
<!-- messung:ende -->

Gemessen wurde die **Live-Seite (main)**, der Code-Audit lief über den **lokalen
Ordner (Zweig)** — die Zahl mischt also zwei Stände, siehe
[90-NOTIZEN.md](90-NOTIZEN.md).

## Die drei wichtigsten offenen Punkte

0. **Zweig `recht/2026-09-18-marke-im-aufbau` (Marke im Aufbau, Verkaufsschalter) mergen und pushen** — erst danach verschwinden Preise, Kasse und „§ 19 UStG" von der Live-Seite. Keine Railway-Variable nötig, die Vorgabe ist aus. Offene Rechtsfragen dazu in [80-AUFGABEN.md](80-AUFGABEN.md), Abschnitt „Verkauf einschalten nach Gewerbeanmeldung".
1. **Den Zweig `sofort/2026-09-18-re15-und-2-weitere` (Paket 248: `RE17`, `RE21`) nach `main` bringen und pushen, danach `/` und `/gaestebuch/` im Browser ansehen** — vor dem Klick darf keine Anfrage an `maps.google.com` gehen, nach dem Klick erscheint die Karte; dieselben Seiten einmal ohne JavaScript (dann bleibt der Rahmen leer), dazu ein Sandbox-Kauf über den Knopf „Zahlungspflichtig bestellen". **Ältere Angaben zum selben Punkt:** den Zweig `sofort/2026-09-17-pj05-und-2-weitere` (Paket 227: `PJ05`, `BF24`) erneut nach `main` bringen und pushen, danach auf `/kontakt/` mit einem Bildschirmleser eine Fehlermeldung auslösen. Paket 219 ist mit `dd804dc`, Paket 223 mit `f8643f0` gemergt; aus Paket 219 bleibt: eine Kontaktanfrage abschicken, im Django-Admin unter „Kontaktanfragen" nachsehen und den ersten Lauf mit Pillow 12.3.0 abwarten. Paket 216 ist mit `8717afe` gemergt; dort steht noch ein Doppelklick-Versuch am Kontaktformular im Browser aus. Die folgenden Angaben stammen vom 12.09.2026 — der Zweig `sofort/2026-09-12-mw15-und-2-weitere` ist inzwischen gemergt, `main` = `origin/main` = `d90067a`; `CANONICAL_HOST` ist laut [80-AUFGABEN.md](80-AUFGABEN.md) Nr. 0 seit dem 16.09.2026 in Railway gesetzt. Durchgesetzte CSP, feste Paketfassungen, Subscriber im Django-Admin (`0c18ea7`), SI09 (`4ec540b`, Nonce statt `'unsafe-inline'`), IS18 (`1a5f36b`, längere belegte Texte auf `/`, `/produkte/` und `/wissen/` — nachmessen), KV07 (`c522ff9`, Danke-Seite `/kontakt/danke/` — nach dem Deploy einmal das Formular abschicken), VL21 (`0d5500b`, Angebotsbausteine unter `shop1/templates/shop1/teile/`) und SI17/PJ01 (`43f25c3`, `integrity` an den vier Fremdskripten, Prüfbefehl `pruefe_links`) sind bereits auf `main` (`69c1f78`), ob Railway sie ausgeliefert hat, ist nicht geprüft; offen sind `4fd7776` (MW15, Prüfbefehl `pruefe_mail`), `be550e3` (GE15, `Article`/`ItemList` im Wissensbereich) und die 301 für `luviq-alsfeld.com` (TS11), deren Vorgabe weiter leer ist. Danach mit offener Browserkonsole die Bezahlseite `/payment/<id>/` und die Ersatzskripte für die früheren `on…`-Attribute (Löschrückfragen, Bestellfilter, Kampagnenknöpfe) ansehen, `pruefe_mail` einmal mit den echten Zugangsdaten fahren und den ersten CI-Lauf mit Django 5.2.17 abwarten — nichts davon ist bisher belegt.
2. **Freigabe der Betreiberin** für die drei **ersten** Wissensbeiträge (Pflegeangaben 30 °C / kein Trockner / kein Weichspüler / Bügeln von links; Faustregel „fünf Zentimeter sind eine ganze Grösse"). Danach je Beitrag `'freigegeben': True` in `shop1/views/wissen.py` — Sitemap, llms.txt und robots-Meta folgen von selbst. `SU04` hängt seit dem 07.09.2026 **nicht mehr** daran: drei belegte Beiträge (Bestellen, Widerruf, Konto) sind ohne Vorbehalt freigegeben und erreichen die Zielgrösse 3. Die Freigabe trifft weiter `SU07`, `SU01` und den grösseren Teil von `SU02`.
3. **Google-Schriften lokal hosten** (RE07, 13 von 13 Seiten laden Inter/Outfit von `fonts.googleapis.com`) — der einzige kritische Rechtsbefund, den der Zweig **nicht** behebt; `base.html` Zeile 212–215. Am 12.09.2026 (Paket 183) erneut angefasst und als **nicht möglich** beendet: im Projekt ist keine `*.woff2`-Datei versioniert, und der Lauf hatte keinen Netzzugang, um die Schnitte zu holen. Braucht einen Lauf, der die Schriftdateien beschaffen darf.

## Zuletzt erledigt

| Datum | Was | Beleg |
|---|---|---|
| 18.09.2026 | `MW22` auf `sofort/2026-09-18-kv14-und-1-weitere` (ein Commit vor `main` = `origin/main` = `d2194e1`, nicht gemergt): der Shop verschickt ohne Umgebungsvariable von `noreply@luviq-alsfeld.com` statt von der nicht existierenden Domain `luviq-shop.de`; `pruefe_mail` und `pruefe_seite` kennen denselben Vorgabewert, ein neuer Test hält ihn. Signiert wird erst, wenn `luviq-alsfeld.com` im Brevo-Konto bestätigt ist. `KV14` nicht möglich (keine geschäftliche Telefonnummer, beim Kunden im Bewertungsblock), `RE03` unklar — für beide keine Zeile Code. Einzelheiten: [80-AUFGABEN.md](80-AUFGABEN.md) „Offen" Nr. 0o | `ed835a9` |
| 18.09.2026 | `PJ06`, `PJ07` und `PJ13` (anders eingebaut) auf `sofort/2026-09-18-pj06-und-2-weitere` (drei Commits, seit dem Merge `d2194e1` auf `main` und `origin/main`). `PJ06`: acht verschluckte Ausnahmen in Kasse, Registrierung und Admin-Panel protokolliert, Newsletter-Rückfall auf `ValueError`/`AttributeError` verengt, direkte Tests für sieben Module. `PJ07`: `signals.py`, `views/auth.py`, `forms.py` aufgeräumt, ohne Verhaltensänderung. `PJ13`: `speakable` per XPath im `WebPage`-Knoten, Antwortsatz der Produktseiten als `antwort_produkt.html`, IndexNow (`shop1/indexnow.py`, `/indexnow-schluessel.txt`) — aus bis `INDEXNOW_KEY` gesetzt ist. Laut `0b94d8e` 314 Tests grün. Einzelheiten: [80-AUFGABEN.md](80-AUFGABEN.md) „Offen" Nr. 0n | `156176e`, `dd339b1`, `0b94d8e` |
| 18.09.2026 | `RE17` und `RE21` auf `sofort/2026-09-18-re15-und-2-weitere` (drei Commits vor `main` = `8114dd9`, nicht gemergt). `RE17`: beide Google-Maps-Rahmen in `_reviews_map.html` tragen ihre Adresse in `data-src` statt in `src`; das Skript am Körperende von `base.html` legt darüber einen Knopf „Karte laden" samt dem Satz „Beim Laden werden Daten an Google Maps übertragen." und setzt `src` erst im Klick. Der Platzhalter entsteht im Skript, nicht in der Vorlage — der ausgelieferte Aufbau bleibt wörtlich derselbe, `aufbau_referenz.json` nicht angefasst; ohne JavaScript bleibt der Rahmen leer. Die Datenschutzerklärung beschreibt seitdem den Platzhalter, `seiten_stand.py` führt `datenschutz` mit dem 18.09.2026. Neue Klasse `EinbettungErstNachKlickTest` in `test_einstellungen` (kein `<iframe>` mit fremdem `src` auf einer öffentlichen Seite, Karte als `data-`Adresse vorhanden, `frame-src` erlaubt sie weiter). `RE21`: der Absendeknopf des Bestellformulars heisst „Zahlungspflichtig bestellen" statt „Continue to Payment →" und deckt sich damit mit `agb.html:42`; nur der Text, keine Klasse, keine Kennung, kein Element. `manage.py check` grün, 290/290 Tests grün laut beiden Commits, im Browser ungeprüft. `RE15` nicht anwendbar: ohne eigenes Cookie, Gerätespeicher, Analysedienst und ohne selbstladende Einbettung gibt es nichts, worüber ein Banner einwilligen liesse — Ausnahme im Bewertungsblock | `d58a96c`, `329dbc4`, `2f51192` |
| 17.09.2026 | `PJ05` (Nachbesserung) und `BF24` auf `sofort/2026-09-17-pj05-und-2-weitere` (zwei Commits vor `main` = `f8643f0`, nicht gemergt). `PJ05`: `_bestaetigung_senden()` in `views/legal.py` protokolliert einen Cache-Fehler der Newsletter-Tagessperre mit `_log.exception` statt `pass`; der Link geht wie bisher trotzdem hinaus. `BF24`: Meldungen in `base.html` mit `role="alert"` (Fehler) bzw. `role="status"`, Kontaktformular mit `aria-live="polite"`; zwei neue Tests, 278 Testfunktionen, laut Bausitzung grün; nur Attribute, Aussehen unverändert, im Bildschirmleser ungeprüft. `PF31` nicht möglich: Alpine, GSAP und Three.js liegen nicht im Projekt und konnten nicht geholt werden, jetzt „beim Kunden" im Bewertungsblock | `a708c7f`, `a8190b8` |
| 17.09.2026 | `PJ05` und `GE43` (Paket 223, beide anders eingebaut), seit dem Merge `f8643f0` auf `main`: begründeter `offen-ok`-Vermerk über `kontakt()`, die vier Wissensseiten verlinken je eine Gesetzesquelle (§ 355 BGB, DSGVO, § 19 UStG). `TS46` beim Kunden (Search Console) | `6b618e4`, `5928144` |
| 17.09.2026 | **Keine automatischen Mails mehr an eingetippte Adressen** (`a437803`). Auf der Agenturseite haben Bots vom 14. bis 17.09.2026 fremde Adressen mit Betrugstexten eingetragen, und die Seite hat sie angeschrieben. **Newsletter mit Double-Opt-in:** Bisher landete jede eingetippte Adresse ohne Rückfrage in der Liste und bekam bei jedem neuen Produkt Post. Jetzt Bestätigungslink `/newsletter/bestaetigen/` (höchstens einer je Adresse am Tag), Newsletter nur an bestätigte Adressen (Migration `0020`). **Alle Abonnenten von vor dem 17.09. sind unbestätigt** und bekommen nichts mehr, bis sie sich neu anmelden. Die Registrierungsmail nennt keinen Namen mehr (er stand unmaskiert im HTML); Registrierung und `resend-verification` sind je IP gedrosselt. 276 Tests grün | [80-AUFGABEN.md](80-AUFGABEN.md) |
| 17.09.2026 | `SI40` und `MW18` auf `sofort/2026-09-17-bf29-und-2-weitere` (zwei Commits vor `main`, nicht gemergt, nicht gepusht). `SI40`: `pillow==12.3.0` statt `11.3.0` in `requirements.txt` und `requirements.lock` — laut `LOGBUCH.md` hatte 11.3.0 acht bekannte Lücken; installiert ist 12.3.0 noch nirgends, die Suite lief mit 11.3.0. `MW18`: neues Modell `KontaktAnfrage` (Migration `0019`, im Django-Admin nur lesen und löschen); `kontakt` speichert die Anfrage vor dem Versand und führt auf `/kontakt/danke/`, sobald sie gespeichert oder versandt ist — nur wenn beides scheitert, bleibt die Fehlermeldung. Sechs neue Tests, 270 Testfunktionen, laut Bausitzung grün; kein Template geändert. Offen: Datenschutzerklärung und Löschfrist (Betreiberin). `BF29` nicht möglich: die Kontrastmängel sitzen im Seitenrahmen und brauchen eine Farbentscheidung der Betreiberin, jetzt als „beim Kunden" im Bewertungsblock | `2cfa8ee`, `0eb7589` |
| 17.09.2026 | `FO03`, `FO04` und `FO07` auf `sofort/2026-09-17-fo03-und-2-weitere` (drei Commits, seit dem Merge `8717afe` auf `main` und `origin/main`). Das Kontaktformular sperrt beim Absenden seinen Knopf (Merkmal `data-einmal-absenden`, Skript in `base.html`, nach der Zurück-Taste wieder frei), die Newsletter-Anmeldung sperrt ihren Knopf bis zur Antwort; auf dem Server führt `doppelt_abgeschickt()` eine wortgleiche zweite Kontaktanfrage binnen zwei Minuten auf die Danke-Seite, ohne zweite Mail (Merker je Gunicorn-Prozess). Die vier Kontaktfelder tragen einen Stern und „(Pflichtfeld)" im `aria-label`, ein Satz im Einleitungstext erklärt den Stern, und `maxlength` entspricht `KONTAKT_LAENGEN`. Acht neue Tests, 264 Testfunktionen, laut Bausitzung grün; kein Element, keine Klasse geändert; im Browser ungeprüft, `seiten_stand.py` für `kontakt` nicht nachgezogen | `3e0a59f`, `370fce7`, `641153b` |
| 16.09.2026 | `FO06`, `FO08` (anders eingebaut) und `FO09` auf `sofort/2026-09-16-fo06-und-2-weitere` (drei Commits, seit dem Merge `d978a89` auf `main`). Kontaktformular und Newsletter prüfen E-Mail-Format und Längen jetzt auf dem Server und nehmen je IP-Adresse höchstens 5 Anfragen in 15 Minuten an (Zähler im `LocMemCache`, also je Gunicorn-Prozess; darüber Status 429). Eine neue Newsletter-Anmeldung schiebt `generate_lead` in den `dataLayer` statt `gtag()` aufzurufen, das die Seite nicht lädt — gezählt wird es erst mit einem Messskript. Neun neue Tests, 252 Testfunktionen, laut `LOGBUCH.md` grün; `pruefe_seite` lief beim Bau nicht, kein Element und keine Klasse geändert | `278e715`, `9b3fc07`, `6aa3087` |
| 12.09.2026 | `MW15` und `GE15` auf `sofort/2026-09-12-mw15-und-2-weitere` (drei Commits, nicht gemergt). `MW15`: dritter Prüfbefehl `pruefe_mail` — er zeigt die Einstellungen **beider** Mailwege (Geheimnisse nur als „gesetzt (n Zeichen)") und versucht die Anmeldung: Brevo-API-Schlüssel gegen die Kontoauskunft `/v3/account` (Lesezugriff, verschickt nichts), SMTP über `get_connection().open()`; `--an ADRESSE` schickt eine Testmail synchron und ohne `fail_silently`, `--ohne-verbindung` lässt jeden Netzzugriff aus, `--streng` zieht Warnungen zu Fehlern. Anlass: beide Wege fallen still aus — der API-Weg protokolliert einen Fehlschlag nur ins Log, während die Bestellung als aufgegeben gilt, und über SMTP läuft die einzige Passwort-vergessen-Mail. Acht neue Tests, wie `pruefe_links` bewusst nicht an `start.sh` angeschlossen; gegen die echten Zugangsdaten noch nie gelaufen. `GE15` (anders eingebaut): die sechs Wissensbeiträge tragen jetzt einen `Article`-Knoten aus einem gemeinsamen Teil-Template (`teile/wissen_article_ld.html`) — `headline`/`description` aus `WISSEN_BEITRAEGE`, `dateModified` aus `seiten_stand.py`, `datePublished` aus dem neuen Registerfeld `veroeffentlicht` (belegt durch `git log --diff-filter=A`), `author`/`publisher` auf `#luisa` und `#organization`; die Übersicht `/wissen/` bekommt bewusst keinen `Article`, sondern eine `ItemList` ihrer Beiträge. Fünf neue Tests, alles im `<head>`, `test_aufbau` unverändert grün. 243 Testfunktionen, laut Commit grün. `GE20` nicht anwendbar (Öffnungszeiten ohne Ladengeschäft), jetzt im Bewertungsblock | `4fd7776`, `be550e3`, `bb6fee1` |
| 12.09.2026 | `SI17` und `PJ01` auf `sofort/2026-09-12-si17-und-2-weitere`, **seit `43f25c3` auf `main` und `origin/main`**. `SI17`: Alpine, `@alpinejs/intersect`, GSAP und das erst im Browser nachgeladene Three.js tragen `integrity="sha256-…"` und `crossorigin="anonymous"` — die Hashes aus der jsDelivr-Dateiauskunft, je zweimal über verschiedene Endpunkte abgefragt und mit dem veröffentlichten jQuery-SRI gegengeprobt, weil der Lauf die Dateien selbst nicht holen durfte und ein falscher Hash Alpine gar nicht mehr laden liesse. Nur zwei Attribute an bestehenden `<script>`-Tags. `PJ01`: der zweite Prüfbefehl `pruefe_links` liest `/`, jede Sitemap- und jede llms.txt-Adresse und ruft jeden `<a href>` darin ab — tote eigene Adressen Fehler, Weiterleitungen Warnungen (ausser auf `LOGIN_URL`), fremde Ziele nur auf `https` geprüft; sechs neue Tests, 230/230 grün laut Commit, nicht an `start.sh` angeschlossen. `SU08` nicht möglich (vier einseitige Bereiche, Inhalte nur von der Betreiberin), jetzt als „beim Kunden" im Bewertungsblock. Offener Rest: die zwei Chart.js-Einbindungen des Admin-Panels ohne veröffentlichten Hash | `680a641`, `11acccc` |
| 12.09.2026 | `VL21` (anders eingebaut) auf `sofort/2026-09-12-vl21-und-2-weitere`, seit dem Merge `0d5500b` auf `main`: die Produktkarte liegt als zwei Bausteine im neuen Ordner `shop1/templates/shop1/teile/` — `angebot.html` für `/produkte/`, `angebot_galerie.html` für den Startseiten-Abschnitt — und wird per `{% include %}` in die bestehenden Schleifen eingebunden. Zwei statt einem Baustein, weil sich Ecken, Bildhöhe, `srcset`, Abzeichen und Textteil der beiden Karten unterscheiden. Gerendertes HTML wörtlich unverändert, `test_aufbau` grün ohne Änderung der Referenz, keine neue Klasse; beide Bausteine laden `custom_tags` selbst (`load` wird an ein `include` nicht vererbt, sonst 500). 224/224 Tests grün laut Commit. `GE11` nicht möglich (zweites Mal, jetzt als „beim Kunden" im Bewertungsblock), `RE07` in diesem Lauf nicht möglich | `5f517de` |
| 11.09.2026 | `KV07` auf `sofort/2026-09-11-kv07-und-2-weitere`, seit `c522ff9` auf `main` (live noch nie abgeschickt): das Kontaktformular leitet nach dem Absenden mit 302 auf die eigene Danke-Seite `/kontakt/danke/` (`noindex, follow`, weder in Sitemap noch in `llms.txt`, `never_cache`), Text nur mit Angaben aus `kontakt.html`; keine bestehende Seite verändert, Designwache-Referenz gezielt ergänzt, `test_formulare` 10 → 16 Tests, 224/224 grün laut Commit. `VL01` als Ausnahme „bewusst so" im Bewertungsblock begründet, kein Umbau. `GE11` nicht möglich — ausser Instagram keine Profiladresse im Projekt. Offen: ein echter Durchlauf des Formulars nach dem Deploy, `EIG10` | `2d78a55`, `7919341` |
| 11.09.2026 | `IS18` (anders eingebaut) auf `sofort/2026-09-11-is18`, seit `1a5f36b` auf `main`: Startseite, Produktübersicht und Wissensübersicht mit belegtem Fliesstext in bestehenden Absätzen auf dem Umfang ihrer Seitenart (laut Commit gezählt wie das Werkzeug, ohne Produkt im Bestand), jede Angabe mit Fundstelle in `LOGBUCH.md`; `MINDESTWOERTER` und `seiten_stand.py` nachgezogen, kein Element, keine Klasse, keine Kennung verändert, 218/218 Tests grün laut Commit. Offen: die fünf Produktseiten (eigene Beschreibung je Stück, Betreiberin), Live-Nachmessung | `2591fde` |
| 11.09.2026 | `SI09` auf `sofort/2026-09-11-si09`, seit `4ec540b` auf `main`: `script-src` ohne `'unsafe-inline'`, Nonce je Anfrage an jedem Inline-Skript und am PayPal-SDK, 17 Handler-Attribute durch `data-`-Attribute ersetzt, drei neue Tests; kein Element, keine Klasse, keine Kennung verändert, 218/218 Tests grün laut Commit. Offen: Verhalten der Ersatzskripte im Browser, `'unsafe-eval'` (Alpine.js) und `'unsafe-inline'` im `style-src` | `28d1ca3` |
| 11.09.2026 | `PJ05`, `SI08`, `PJ11` auf `sofort/2026-09-11-pj05-und-2-weitere`, seit `0c18ea7` auf `main`: kritische Audit-Funde abgearbeitet oder mit Vermerk im Code begründet, `Subscriber` im Django-Admin; `CSP_MODUS`-Vorgabe `scharf`, PayPal-Hosts nach PayPals Angabe; jede Abhängigkeit mit `==` plus `requirements.lock`, Django 5.2.17. Kein Template berührt, 215/215 Tests grün laut Commits. Offen: Bezahlseite mit Browserkonsole nach dem Deploy, erster CI-Lauf mit 5.2.17 | `9ebad08`, `9a3226f`, `51cbf74` |
| 08.09.2026 | `GE23`, `GE25`, `IS19`: Antwortabsatz mit belegter Zahl auf jeder Inhaltsseite, konkrete Zahlen auf den fünf Seiten ohne, keine indexierbare Seite mehr unter 200 Wörtern im Inhaltsbereich — ohne eine Änderung am Aufbau, 215/215 Tests grün. Offen bleibt eine eigene Beschreibung je Einzelstück | `fd82efd`, `2b26108`, `b35f6e4` |
| 07.09.2026 | `SU04`: drei belegte Wissensbeiträge ohne Freigabevorbehalt (Bestellen und Bezahlen, Widerruf und Rücksendung, Konto und Daten); `/wissen/` damit indexierbar, Sitemap und llms.txt führen vier Wissensadressen | `d7d2e0b` |
| 02.09.2026 | Auflagen der Gegenprüfung (1, 3, 4, 5) umgesetzt; Lauf 4 damit „live-fertig", Zweig gepusht | `b750337`, `60555d0`, `270c5f9`, `511ffe5` |
| 02.09.2026 | Welle 9: Warenkorb-, Zahlungs-, Konto-, Zugriffsschutz- und Invarianten-Tests (174 → 215 Tests) | `87a4e85` … `2e9b051` |
| 02.09.2026 | Welle 8: CSP-Middleware (Report-Only), Canonical-Host-Middleware, `pruefe_seite` in `start.sh` | `3a9c2d3`, `6bd5eb4`, `50d68da` |
| 01.09.2026 | Wellen 1–7: Barrierefreiheit, Meta, Schema-Knoten, Inhalt, Wissensbereich, WebP, GZip, Gunicorn gthread | `65a1bd0` … `b0fba20` |
| 01.09.2026 | Lauf 3, Schritte 32–33: toter Code entfernt, Doku an den Code angeglichen, `LOGBUCH.md` angelegt | `c6e0cce`, `342f7fd` |
| 18.08.2026 | README im GitHub-Repo, letzte Railway-Auslieferung | `645842b` (Railway) |
| 15.07.2026 | Startseite PageSpeed und Politur; `CLAUDE.md` versioniert — **letzter Stand auf main** | `2fe90a0`, `2a17edd` |
| 13.07.2026 | Admin-Mail pro Seitenbesuch vollständig entfernt (Mail-Flut durch Bots) | `e58775a`, `26b6e72` |
