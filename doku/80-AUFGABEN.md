---
bereich: aufgaben
titel: Aufgaben
stand: 2026-09-12
status: teilweise
fortschritt: 50
zusammenfassung: SU04, GE23, GE25, IS19, PJ05, SI08, PJ11, SI09, IS18, KV07, VL01 und seit dem Merge 0d5500b auch VL21 liegen auf main (7f5ef63; origin/main steht vier Doku-Commits zurück auf f90d791, ob Railway ausgeliefert hat, ist nicht geprüft; SI09 und die Danke-Seite im Browser ungeprüft). Paket 196 (12.09.2026, Zweig sofort/2026-09-12-si17-und-2-weitere, zwei Commits 680a641 und 11acccc, nicht gemergt) bringt SI17 und PJ01: die vier Fremdskripte von jsDelivr (Alpine, intersect, GSAP und das nachgeladene Three.js) tragen integrity und crossorigin, und neben pruefe_seite steht der zweite Prüfbefehl pruefe_links, der jeden Verweis jeder öffentlichen Seite abruft — 230/230 Tests grün laut Commit, kein Template-Aufbau berührt. SU08 im selben Paket nicht möglich und seitdem als beim Kunden im Bewertungsblock: die vier einseitigen Bereiche brauchen Inhalte, die nur die Betreiberin liefern kann. Offener Rest von SI17 sind die zwei Chart.js-Einbindungen des Admin-Panels ohne veröffentlichten Hash. Nächster Schritt ist der Merge dieses Zweigs samt Push, danach Nachmessung, Bezahlseite und Ersatzskripte mit offener Browserkonsole und der erste CI-Lauf mit Django 5.2.17; Schriften lokal, Sprungmarke, Icon-Knöpfe, Permissions-Policy und die Freigaben der Betreiberin bleiben offen.
offen: 6
quellen: LOGBUCH.md, CLAUDE.md, GOOGLE_SEO_GUIDE.md
---

# Aufgaben — Luviq Universe

*Woran sich der Fortschritt bemisst: am Anteil der erledigten an allen in dieser Datei geführten Aufgaben — „Erledigt“ gegen „Erledigt + Offen + Fehlt + Beim Kunden“, auf Zehner gerundet. „Verbesserungsmöglichkeiten“ zählen nicht mit, sie sind Kür, keine Zusage. Bei allen sechs betreuten Seiten dieselbe Rechnung.*

Regelkennungen verweisen auf die **Messung vom 02.09.2026 (Regelstand 2026-09-02a)**.
„Offen" = konkret als Nächstes · „Fehlt" = noch nicht begonnen ·
„Verbesserungsmöglichkeiten" = die grössten Hebel aus den offenen Regeln ·
„Beim Kunden" = braucht Zuarbeit der Betreiberin · „Erledigt" = mit Datum.

## Missbrauchsschutz am Formular (Erhebung 04.09.2026)

Aus dem Pflichtabschnitt in [10-TECHNIK.md](10-TECHNIK.md). Jede Zeile ist ein
Baustein, der im Quelltext dieses Projekts **nicht** gefunden wurde. Die
Hauptseite führt alle; was dort steht, lässt sich übernehmen.

- [ ] **Honeypot**
- [ ] **Zeitfalle (signierter Zeitstempel)**
- [ ] **Inhalts-Score mit Schwelle**
- [ ] **Adresse ohne `http://` erkannt**
- [ ] **Fremde Domain mit eigenem Markennamen**
- [ ] **Mail-Obergrenze je Tag**
- [ ] **Prüfbefehl für die Abwehr**

## Offen

Alles hier ist vorbereitet und wartet nur auf Ausführung.

| # | Aufgabe | Warum jetzt | Wo |
|---|---|---|---|
| 1 | **Arbeitszweig `sofort/2026-09-12-si17-und-2-weitere` nach `main` mergen und pushen** (zwei Commits vor `main`: `680a641` `SI17`, `11acccc` `PJ01`; 230/230 Tests grün laut Commit) | Der Vorgängerzweig `sofort/2026-09-12-vl21-und-2-weitere` (`5f517de` `VL21`) ist mit dem Merge `0d5500b` in `main` enthalten (geprüft 12.09.2026). **`main` steht auf `7f5ef63` und ist `origin/main` (`f90d791`) um vier Doku-Commits voraus — der Push steht also schon für den heutigen Stand aus**; ob Railway ausgeliefert hat, ist hier nicht geprüft. `SI17` ändert nur zwei Attribute an vier `<script>`-Tags, `PJ01` fügt einen Prüfbefehl und sechs Tests hinzu — am gerenderten Aufbau ändert sich nichts, der Merge eilt nicht, aber `SI17` schliesst eine Sicherheitslücke, die auf jeder Seite wirkt. **Nicht gemergt bleibt ausserdem** `sofort/2026-09-12-re07-und-2-weitere` (Paket 188, fünf Commits) — er unterscheidet sich von `main` nur noch in `LOGBUCH.md` und `doku/` (`git diff --stat`, geprüft 12.09.2026), **Code steht dort keiner offen**: `BF08` ist in der Nachbesserung `d4e2483` bewusst zurückgenommen, siehe Bewertungsblock. `/kontakt/danke/` ist damit im Hauptzweig, aber noch nie live abgeschickt worden (siehe Nr. 2 e). `VL21` ändert am gerenderten HTML nichts, der Merge ist deshalb nur Ordnung im Code — er eilt nicht. **Zweiter Zweig mit dem SI09-Code:** `sofort/2026-09-11-si09-und-2-weitere` (Paket 162, `4f3ac5f`) ist nicht gemergt; laut `LOGBUCH.md` (Paket 164) ist `28d1ca3` über `mainweb/`, `shop1/`, `templates/` und `CLAUDE.md` wortgleich, damit ein späteres Zusammenführen keinen Konflikt erzeugt. **`TS11` ist weiter nicht dabei:** `CANONICAL_HOST` hat auch hier die leere Vorgabe (`mainweb/settings.py:45`) — `CANONICAL_HOST=www.luviq-alsfeld.com` bleibt in Railway zu setzen | `git merge` + Push, Railway baut selbst |
| 2 | **Nach dem Push und Deploy prüfen:** (a) der erste CI-Lauf mit Django 5.2.17, (b) mit offener Browserkonsole `/payment/<id>/` (PayPal-SDK mit `nonce` und `data-csp-nonce`) und die Ersatzskripte für die früheren Handler-Attribute: Löschrückfrage im Profil, „Reset" in der Panel-Statistik, Bestellfilter in `/shop-admin/`-Bestellungen, Kampagnenknöpfe (Neu, Bearbeiten, Abbrechen, Löschen, Views/Klicks) in der Kampagnenliste, Nachladen der Google-Schriften, Ausblenden fehlender Werbebilder, (c) nachmessen, dass jede Seite `Content-Security-Policy` mit einer Nonce und ohne `'unsafe-inline'` im `script-src` sendet, (d) nachmessen, ob `/`, `/produkte/` und `/wissen/` mit `IS18` (seit `1a5f36b` auf `main`) den Umfang ihrer Seitenart erreichen, (e) nach dem Deploy des Merges `c522ff9` (`KV07`) einmal das Kontaktformular abschicken: Weiterleitung auf `/kontakt/danke/`, Seite mit `noindex, follow`, Mail kommt an | (a) Die lokale Suite lief beim Bau mit Django 6.0.5, nicht mit der festgenagelten 5.2.17 — `pip install` und `docker build` waren gesperrt (`LOGBUCH.md`, Paket 155); erster Nachweis ist `.github/workflows/pruefungen.yml` (Python 3.12, bei jedem Push). (b) Die Deckung der Positivliste und die Nonce an jedem Inline-Skript sind nur durch Tests gegen die gerenderten Vorlagen belegt, das Verhalten der Ersatzskripte gar nicht („im Browser nicht geprüft", `28d1ca3`) — eine Rückfrage, die nicht erscheint, heisst: Löschen ohne Nachfrage. Blockiert die Richtlinie etwas, `CSP_MODUS=report-only` in Railway setzen — wirkt ohne neuen Stand. (c) Gemessen wird die Live-Seite. (d) Der Bau hat nur lokal gezählt, nach dem Zählverfahren des Werkzeugs nachgebaut und ohne ein einziges Produkt im Bestand; live gezählt ist der Stand nicht (`LOGBUCH.md`, Paket 165). (e) Die Tests ersetzen den Versand durch eine Attrappe; ob Brevo live zustellt, zeigt nur ein echter Durchlauf — die Danke-Seite erscheint auch, wenn der Versand erst im Thread scheitert (`EIG10`) | GitHub Actions, Browser, Werkzeug-Messlauf |
| 3 | **Google-Schriften lokal hosten** (Inter 400–700, Outfit 700/900 als WOFF2, `@font-face`, `font-display: swap`) | `RE07` ist der einzige **kritische** Rechtsbefund, den der Zweig nicht behebt: 13 von 13 Seiten laden von `fonts.googleapis.com`, die Adresse des Besuchers geht vor jeder Einwilligung zu Google. **Am 12.09.2026 (Paket 183) erneut angefasst und als nicht möglich beendet:** im Projekt ist keine einzige `*.woff2`-Datei versioniert (`git ls-files`), und der Lauf hatte keinen Netzzugang, um die Schriftschnitte zu holen — weder `curl` noch `urllib` waren freigegeben. Eine halbe Umstellung (nur Inter selbst hosten, Outfit weiter von Google) bringt rechtlich nichts: die Adresse des Besuchers geht trotzdem zu Google. Keine Zeile Code geändert | `templates/base.html`, Zeilen 212–215 (Preconnect, `data-schrift-nachladen`, `noscript`); die Schriftdateien müssen zuerst beschafft und nach `shop1/static/shop1/` gelegt werden |
| 4 | **Sprungmarke „Zum Inhalt"** als erstes fokussierbares Element im `body`, Ziel-`id` im `<main>` | `BF08`/`VL17`: 13 von 13 Seiten ohne. Änderung am Seitengerüst → Designwache-Referenz bewusst nachziehen | `templates/base.html`, `aufbau_referenz.json` |
| 5 | **Menüknopf und Icon-Knöpfe benennen** (`aria-label`), dazu die 5 namenlosen Links auf `/produkte/` | `BF12` (13 namenlose Schaltflächen), `BF11`, `VL18` — reine Attributarbeit, die Designwache bleibt grün | Templates |
| 6 | **`Permissions-Policy`-Kopfzeile** setzen (mindestens `geolocation=()`, `camera=()`, `microphone=()`, dazu `payment=()`, `usb=()`, `browsing-topics=()`) | `SI07`, `VL04`: fehlt auf 14 von 14 Seiten; die CSP-Middleware ist die passende Stelle | `shop1/middleware.py` |

## Fehlt

Noch nicht begonnen — kein Code, kein Plan, kein Termin.

| Bereich | Was fehlt | Regel |
|---|---|---|
| **Tor** | **Ein einziger Eintrag in `sites.json` → `pruefbefehle`** — die Liste ist **leer**, und damit fährt das Paket-Tor an dieser Seite **keinen** Prüfbefehl: `tor_hindernisse()` prüft Bedingung 3 als `if rot:`, und was nie gefahren wurde, ist nie rot. Ein Paket passiert hier mit „0 von 0 grün“. Die Testsuite ist vorhanden und **grün** (215 Funktionen, 137 s, gemessen 06.09.2026); es fehlt nur `"python manage.py test"` in der Liste. Erhebung über alle sechs Seiten: `pystore-overview/docs/BEFUNDE-UEBERTRAGBAR.md` **B22** | — (Werkzeugseite) |
| Qualitätssicherung | Fehler-Monitoring (Sentry o. ä.). **`PJ01` ist seit dem 12.09.2026 erledigt** (zweiter Prüfbefehl `pruefe_links`, siehe „Erledigt"), ein CI-Lauf bei jedem Push und Pull Request steht in `.github/workflows/pruefungen.yml` | `VL19` |
| Auslieferung | `runtime.txt`, `railway.json`. Das Festnageln mit `==` samt Lockfile ist seit dem 11.09.2026 im Zweig erledigt (`51cbf74`, siehe „Erledigt") | `VL02` |
| Entwicklungsrechner | Auf die festgenagelten Fassungen ziehen (`pip install -r requirements.txt`) — dort war am 11.09.2026 noch Django 6.0.5 installiert (Kommentar in `requirements.txt`). Der alte Unterschied Container (Django 5.2) gegen Testsuite (6.0) ist im Zweig durch `Django==5.2.17` aufgelöst: Container (`python:3.11-slim`) und CI (Python 3.12) bauen dieselbe Fassung; belegt erst mit dem ersten CI-Lauf („Offen" Nr. 2) | Kommentar in `requirements.txt`, `LOGBUCH.md` Paket 155 |
| Fehlerseite | Gestaltete 404-Seite mit Navigation (heute Django-Standard, 13 Wörter) | `BT05`, `TS20` |
| Sicherheit | **Rest von `SI17`:** die zwei Chart.js-Einbindungen des Admin-Panels. `admin/werbung_list.html:365` lädt `chart.js@4.4.0/dist/chart.umd.min.js` — eine Datei, die es im Paket nicht gibt und die jsDelivr beim Abruf selbst erzeugt; für erzeugte Dateien gibt es keinen veröffentlichten Hash. `admin/stats.html:246` lädt `chart.js` **ganz ohne Fassung**, also eine Datei, die sich jederzeit ändern darf. Beide abzusichern hiesse, die Einbindung auf eine andere Datei umzustellen — eine Fassungsänderung am Panel, die eine eigene Entscheidung ist. Die vier Fremdskripte der öffentlichen Seiten sind seit dem 12.09.2026 abgesichert (siehe „Erledigt") | `SI17` |
| Sicherheit | **Rest von `SI09`:** `'unsafe-eval'` im `script-src` — Alpine.js 3.14.8 (Standardfassung) wertet jeden `x-data`-/`@click`-/`x-intersect`-Ausdruck mit `new Function` aus; laut `LOGBUCH.md` (Paket 164) 224 Treffer auf Alpine-Attribute in 19 Vorlagen. Ohne das Schlüsselwort braucht es die CSP-Fassung von Alpine, und die verlangt jeden Ausdruck als registrierte Komponente neu geschrieben — nur mit Browser prüfbar. Dazu `'unsafe-inline'` im `style-src` (`style="…"`-Attribute, `<style>`-Blöcke, PayPal-SDK). SI09 ist deshalb mit dem Merge nicht voll erfüllt | `SI09` |
| Recht | Muster-Widerrufsformular als eigene, aus dem Fuss verlinkte Seite; Erklärung zur Barrierefreiheit (BFSG), sofern die Kleinstunternehmens-Ausnahme nicht greift — ob sie greift, ist **nicht dokumentiert** | `RE09`, `RE12` |
| Formular | Honigtopf im Kontaktformular; Datenschutzhinweis unter dem Formular | `KV06`, `KV05` |
| GEO | `speakable`, Antwort-zuerst-Baustein als Vorlagenteil, IndexNow, `llms-full.txt` | `VL09`, `PJ13`, `GE19`, `GE31` |
| Sitemap | Segmentierung in mehrere Klassen, Sitemap-Index, Bild-Erweiterung für alle Einträge | `VL07`, `TS19` |
| Bilder | Produktbilder aus Cloudinary als WebP/AVIF und mit `srcset` (der Zweig fasst nur die statischen Bilder an) | `PF15`, `PF16`, `VL15` |
| Betrieb | `STRIPE_*`-Variablen in Railway entfernen — welche Namen dort genau stehen, ist nicht dokumentiert | — |

## Verbesserungsmöglichkeiten

Die grössten Hebel aus den offenen Regeln der Messung vom 02.09.2026. Der schwächste Bereich ist
**Substanz & Reichweite mit 43,6 („Lückenhaft“)** — dort liegt fast alles, was den Gesamtstand von 75,8 nach oben zieht.

### Substanz — der grösste Hebel

| Regel | Befund | Was es braucht |
|---|---|---|
| **`SU02`** | **1.557 Eigenwörter über 13 Seiten, Zielgrösse 12.000** (von 2.434 Wörtern Gesamttext); umfangreichste Seiten `/` (390 W), `/liefergebiet/` (259 W), `/ueber_uns/` (194 W) | Auskunft statt Werbung: Abläufe, Preisrahmen, echte Fälle, beantwortete Fragen. Die drei ersten Wissensbeiträge im Zweig bringen zusammen rund 2.650 Wörter, warten aber auf die Freigabe; die drei aus SU04 bringen 3.163 Wörter (1.129 + 1.034 + 1.000, gemessen 07.09.2026, festgehalten in `MINDESTWOERTER`) und zählen ohne Rückfrage mit, sobald der Zweig live ist |
| **`SU01`** | **13 rankfähige Seiten, Zielgrösse 30** für diese Geschäftsart | Je wiederkehrender Kundenfrage eine Seite. Bestellablauf, Widerruf und Konto sind mit SU04 gebaut; offen bleiben Themen, die Angaben der Betreiberin brauchen (Materialien, Preisrahmen, Rückporto) |
| **`SU04`** | **0 Wissensseiten, Zielgrösse 3** — kein einziger Ratgeberbereich | **Im Zweig erfüllt** (07.09.2026, `d7d2e0b`): drei Beiträge, deren jede Sachangabe aus AGB, Datenschutz, Impressum, Liefergebiet oder dem Shop-Code belegt ist, stehen auf `freigegeben: True` — `/wissen/bestellen-und-bezahlen/`, `/wissen/widerruf-und-ruecksendung/`, `/wissen/konto-und-daten/`. Sie brauchen keine Zuarbeit; Sitemap und `llms.txt` führen sie samt Übersicht (vier Adressen), `/wissen/` ist dadurch indexierbar. Live zählt der Punkt erst nach dem Merge |
| `SU07` | 0 Wissensseiten auf 6 Verkaufsseiten; Ziel eine je drei | Die drei belegten Beiträge zählen ab dem Merge mit; die drei gesperrten erst nach der Freigabe |
| `SU06` | im Schnitt 120 Eigenwörter je Seite, Ziel 400; dünnste: Produktseiten mit 25 W | Echte Produktbeschreibungen der Betreiberin. Der Zweig ergänzt nur den für alle Produkte gleichen Zusatz — mit IS19 (08.09.2026) ist dieser Zusatz länger geworden, aber auf allen fünf Stücken **derselbe Text**. Das hebt die Wortzahl, nicht die Eigenständigkeit: die Textgleichheit zwischen den Produktseiten (`IS21`) sinkt dadurch nicht |
| `SU08` · `SU09` · `SU10` | 4 von 5 Themenbereichen mit nur einer Seite; `/produkt/` ohne Übersichtsseite (Zweig: 301 auf `/produkte/`); `/produkt/` hält 56 % aller Unterseiten | Bereiche ausbauen statt weitere Einzelseiten anlegen. **`SU08` am 12.09.2026 (Paket 196) geprüft und als nicht möglich beendet**, keine Zeile Code geändert: einseitig sind `/produkte/`, `/gaestebuch/`, `/ueber_uns/`, `/liefergebiet/`. `/produkte/` ist die Übersicht des Produktbereichs, dessen Detailseiten unter `/produkt/<slug>/` liegen (`shop1/urls.py`) — beide zusammenzulegen hiesse, jede indexierte Produktadresse umzuziehen. Für die drei anderen fehlt der Inhalt einer zweiten Seite: `Produkt` hat kein Kategoriefeld (`shop1/models.py`), und das Register `shop1/seiten_stand.py` führt genau 16 Seiten. Steht seitdem als **beim Kunden** im Bewertungsblock, siehe „Beim Kunden" Nr. 16 |

### Inhalt und Titel

| Regel | Befund | Was es braucht |
|---|---|---|
| **`IS19`** | **85 % der Seiten sind dünn (11 von 13)**: `/produkte/` 62 W, `/kontakt/` 91 W, `/datenschutz/` 185 W, `/agb/` 179 W, `/gaestebuch/` 70 W … (+6) | **Im Zweig behoben** (08.09.2026, `b35f6e4`): keine indexierbare Seite liegt noch unter 200 Wörtern im Inhaltsbereich (gemessen mit einem Produkt und ohne Kommentare). `/impressum/` bleibt bei 75 Wörtern — es trägt `noindex` und steht weder in der Sitemap noch in `llms.txt`. Live gilt weiter der Messwert |
| **`IS18`** | **Umfang passt nicht zur Aufgabe: 7 von 7 Seiten darunter** — `/` 390/700 W, `/produkte/` 62/600 W, Produktseiten je 25/600 W | **Im Zweig für drei Seitenarten anders eingebaut** (11.09.2026, `2591fde` auf `sofort/2026-09-11-is18`, nicht gemergt): `/` (Ziel 700), `/produkte/` (Ziel 600) und `/wissen/` (Ziel 900) geben die Beschreibung je Stück gar nicht aus und erreichen ihr Ziel jetzt mit belegter Auskunft in Absätzen, die es schon gab — laut Commit gezählt wie das Werkzeug und **ohne ein Produkt im Bestand**, damit der Umfang nicht mit jedem verkauften Stück wieder fällt. `MINDESTWOERTER` hält den Stand fest (`/` 690, `/produkte/` 595, `/wissen/` 895). **Offen bleiben die fünf Produktseiten:** ihr statischer Teil ist auf allen Stücken wortgleich, noch mehr gleicher Text höbe zugleich die Textgleichheit (`IS21`); ihren Umfang kann nur eine eigene Beschreibung je Stück liefern (`SU06`, „Beim Kunden" Nr. 14). Live gilt weiter der Messwert |
| `IS17` | 12 von 13 Seiten unter 300 Eigenwörtern | dito |
| **`IS23`** | **Kannibalisierung: „custom print" auf 3 Seiten** (`/produkt/custom-print-hoodie-1/`, `/produkt/custom-print-jacke/`, `/produkt/custom-print-hoodie/`), 3 von 5 Seiten betroffen | Je Begriff eine Seite: Produktnamen im Shop-Admin unterscheidbar machen oder `seo_titel` setzen; zwei Produkte tragen sogar denselben Titel (`IS03`, `BF21`) |
| `IS02` · `IS09` · `IS11` · `IS06` | Titel 7 von 13 in der Zielspanne, Beschreibungen 5 von 13, Handlungsaufforderung in 4 von 13, Ort oder Nutzen im Titel bei 2 von 13 | Grösstenteils im Zweig erledigt (Schritte 11–13); nach dem Merge nachmessen |
| `IS14` | 10 von 13 Seiten springen `h1 → h3` | Nur mit bewusster Änderung der Designwache-Referenz |

### Recht und Vertrauen

| Regel | Befund | Was es braucht |
|---|---|---|
| **`RE06`** | **3 von 3 eingebundenen Diensten fehlen in der Datenschutzerklärung**: `cdn.jsdelivr.net`, `fonts.googleapis.com`, `maps.google.com` | **Im Zweig behoben** — die Erklärung nennt jetzt Cloudinary, Google Fonts, Google Maps, Railway, jsDelivr, ip-api, Brevo und PayPal. Live steht noch die alte Fassung (nur PayPal, Railway) |
| **`RE07`** | **13 von 13 Seiten laden Google-Schriften von fremden Servern** (`fonts.googleapis.com`, Inter und Outfit) | Selbst hosten — **nicht** im Zweig behoben; am 12.09.2026 (Paket 183) als **nicht möglich** beendet, weil keine Schriftdatei im Projekt liegt und der Lauf keinen Netzzugang hatte, siehe „Offen" Nr. 3 |
| `RE03` | Im Impressum nicht auffindbar: Anbietername oder Rechtsform, Strasse mit Hausnummer (gefunden: PLZ mit Ort, E-Mail, zweiter Kontaktweg) — der Prüfer erkennt „Luisa Brehler" und „Grünberger Str. 16" nicht als solche | Angaben klarer auszeichnen; inhaltlich sind sie vorhanden |
| `RE09` | Widerrufsbelehrung in `/agb/`, aber ohne Muster-Widerrufsformular | Eigene Seite, aus dem Fuss verlinkt |

### Konversion und Barrierefreiheit

| Regel | Befund | Was es braucht |
|---|---|---|
| **`KV01`** | **1 von 13 Seiten mit `tel:`-Link** — und dieser eine ist die Platzhalternummer „+49 (0) 30 123456" auf `/kontakt/` (main). Nach dem Merge sind es 0 | Nur die Betreiberin kann eine echte Nummer nennen (siehe „Beim Kunden"). Ohne Nummer bleibt die Regel offen — eine zu erfinden ist ausgeschlossen |
| **`BF05`** | **5 von 5 Formularfeldern ohne Beschriftung**: `/` (1), `/kontakt/` (4) | **Im Zweig behoben** (Schritt 6, `4f0f878`: jedem Feld ein zugänglicher Name; Auflage 1 korrigierte acht zusammengeklebte Attribute im Checkout) — live noch offen |
| `KV09` | 2 von 6 Vertrauenssignalen auf der Startseite; es fehlen Zertifikate, Referenzen, Jahreszahl, Bewertung im Schema | Nur mit belegbaren Angaben — die sichtbaren „5.0 ★★★★★" sind nicht belegt und dürfen nicht ins Schema |
| `KV05` · `KV06` · `KV07` | Formular ohne Datenschutzhinweis, ohne Honigtopf, ohne Danke-Seite | **`KV07` im Zweig erledigt** (11.09.2026, `2d78a55` auf `sofort/2026-09-11-kv07-und-2-weitere`, nicht gemergt): eigene Adresse `/kontakt/danke/`, siehe „Erledigt". `KV05` und `KV06` siehe „Fehlt" |
| `BF08` · `BF12` · `BF15` | Keine Sprungmarke, 13 namenlose Schaltflächen, 10 Seiten mit Überschriftensprung | Siehe „Offen" Nr. 4 und 5 |

### Code und Projektgerüst

| Regel | Befund | Was es braucht |
|---|---|---|
| **`PJ05`** | **2 kritische Datei-Befunde der Messung vom 04.09.2026: `mainweb/settings.py:59` — `ALLOWED_HOSTS` steht auf `'*'` — und `shop1/middleware.py:218` — Ausnahme wird verschluckt** | **Im Zweig abgearbeitet** (11.09.2026, `9ebad08` auf `sofort/2026-09-11-pj05-und-2-weitere`; Zeile für Zeile derselbe Code wie `e36f4bc` vom 08.09.2026, der nie ausgeliefert wurde). Acht Funde des Code-Audits an ihrer Stelle: `_geo_enrich` in `shop1/middleware.py` schreibt die Ausnahme beim Schliessen der pystore-Verbindung jetzt als Warnung ins Protokoll (`P02`; Ablauf unverändert, der Platz wird weiter im `finally` freigegeben); `Subscriber` ist in `shop1/admin.py` registriert (`V11`: Liste, Suche nach Adresse, Filter nach Datum, `erstellt_am` schreibgeschützt — eine Auskunft oder Löschung nach DSGVO braucht keinen Datenbankzugriff mehr; das Panel unter `/shop-admin/` bleibt unverändert). Sechs Stellen bleiben bewusst, wie sie sind, und tragen dafür einen Vermerk im Code — **anders eingebaut** als eine Anmeldepflicht, die Besucher aussperrte: `ALLOWED_HOSTS = ['*']` steht nur im `DEBUG`-Zweig (`# audit-ok K02:`, Test `test_die_hostliste_ist_nicht_offen`), und fünf Stellen ohne Anmeldeschutz (`K06`: `_sync_session_to_db` in `views/_helpers.py`, `register` in `views/auth.py`, `newsletter_subscribe` in `views/legal.py`, `startseite` und `werbung_klick` in `views/shop.py`) tragen je ein `# offen-ok:` mit dem Grund. Bewusst **keine** Ausnahme im Bewertungsblock — sie nähme jeden künftigen kritischen Befund mit aus der Wertung (`LOGBUCH.md`, Paket 155). Ob der nächste Messlauf die Vermerke anerkennt, zeigt erst die Messung |
| **`VL01`** | **Projektgerüst: 1 von 6 Merkmalen erfüllt** — es fehlen `config/settings.py`, `config/urls.py`, `config/wsgi.py`, ein `apps/`-Paket statt Wurzelmodulen und reine Datenmodule (`data/`) | **Bewusst nicht angefasst.** Der Umbau von `mainweb/` + `shop1/` auf `config/` + `apps/` berührt jeden Import, jede Migration und den Startbefehl — bei der einzigen Seite mit echten Bestellungen ein unverhältnismässiges Risiko für einen Formregelpunkt. Seit dem 11.09.2026 steht das als **Ausnahme „bewusst so"** mit Belegen im Bewertungsblock unten (`7919341`, Zweig `sofort/2026-09-11-kv07-und-2-weitere`, nicht gemergt; nur diese Datei geändert, kein Code). Wenn überhaupt, dann als eigener Lauf mit Sandbox-Test |
| **`VL21`** | **Kein eigener Angebotsbaustein** — die Karte, mit der der Shop sein Angebot zeigt, stand zweimal mitten in einer Vorlage (`produkte.html` 72–115, `index.html` 486–507) | **Im Zweig anders eingebaut** (12.09.2026, `5f517de`): zwei Bausteine statt des einen, den die Regel vorschlägt — `shop1/templates/shop1/teile/angebot.html` (Produktübersicht) und `teile/angebot_galerie.html` (Startseiten-Abschnitt „Aktuelle Unikate"), je per `{% include %}` in der bestehenden Schleife. Ein gemeinsamer Baustein hätte das Aussehen einer der beiden Ansichten geändert (Ecken, Bildhöhe, `srcset`, Abzeichen, Aufbau des Textteils unterscheiden sich) oder sechs Verzweigungen gebraucht; Einzelheiten in „Erledigt" |
| `PJ11` · `VL02` | 11 von 11 Abhängigkeiten ohne feste Fassung, kein Lockfile, kein `runtime.txt`, kein `railway.json` | **`PJ11` im Zweig erledigt** (11.09.2026, `51cbf74`): jede Zeile der `requirements.txt` mit `==`, die Unterabhängigkeiten in `requirements.lock`, die `requirements.txt` selbst per `--constraint` einbindet — so gilt sie im Container, im CI-Lauf und lokal; der `Dockerfile` kopiert beide Dateien. Wer eine Fassung ändert, schreibt die Lockdatei neu (Anleitung im Kopf von `requirements.lock`). **Werkzeuglücke:** das Code-Audit liest die Endung `.lock` nicht und meldet deshalb weiter „kein Lockfile" (`LOGBUCH.md`, Paket 155). `runtime.txt` und `railway.json` (`VL02`) siehe „Fehlt" |
| `PJ03` | 43 von 53 Python-Modulen von einem Test berührt | Die zehn gemeldeten Module sind über Seiten-, Konto- und Zahlungstests indirekt abgedeckt; das Audit zählt nur direkte Importe |
| `VL19` | 3 von 7 QS-Bausteinen | Siehe „Fehlt" |

## Beim Kunden

Braucht Zuarbeit von **Luisa Brehler**. Nichts davon darf erfunden oder geschätzt werden.

| # | Was | Warum es hängt | Was danach passiert |
|---|---|---|---|
| 1 | **Freigabe der Pflegeangaben:** Waschen auf links bei **30 °C**, **kein Trockner**, **kein Weichspüler**, **Bügeln nur von links** | Die Angaben sind im Projekt nirgends belegt; auf der eigenen Shopseite liest man eine Pflegeanleitung als Anweisung der Verkäuferin — eine falsche Angabe ruiniert ein Einzelstück | `'freigegeben': True` beim Beitrag `pflege-handbemalte-kleidung` in `shop1/views/wissen.py` |
| 2 | **Freigabe der Grössen-Faustregel:** „fünf Zentimeter Unterschied in der Brustweite sind eine ganze Grösse" | Dieselbe Begründung; die Regel entscheidet über Rücksendungen | `'freigegeben': True` bei `groesse-bei-einzelstuecken` |
| 3 | **Freigabe des Upcycling-Beitrags** (keine strittige Zahl darin, die Auflage nennt aber alle drei Beiträge) | Gegenprüfung Lauf 4, Auflage 3 | `'freigegeben': True` bei `upcycling-mode-second-hand-vintage` |
| | **Wirkung der drei Freigaben zusammen** | | Die Beiträge verlieren `noindex, follow` und kommen in Sitemap und `llms.txt`. Sitemap, llms.txt, robots-Angabe und die Tests folgen dem Register **von selbst** — es ist je Beitrag ein Wort. Trifft `SU07`, `SU01`, `SU02`, `VL11`, `VL12`. **Nicht mehr davon abhängig:** `SU04` und die Indexierbarkeit von `/wissen/` — beides ist seit SU04 (07.09.2026) durch die drei belegten Beiträge erfüllt |
| 4 | **Telefonnummer** — gibt es eine geschäftliche? | `KV01`, `GE09`, `VL10`, `RE03`: der Betriebsknoten im Schema hat kein Telefon, keine Seite einen `tel:`-Link. Die Platzhalternummer wurde entfernt, statt eine zu erfinden | `tel:`-Link in Kopf und Fuss, `telephone` im Schema, Ergänzung im Impressum |
| 5 | **Erreichbarkeits- oder Antwortzeiten** | `KV11`: „Operationell: 24/7" auf `/kontakt/` (main) ist Platzhaltertext | Angabe auf Start- und Kontaktseite, `openingHoursSpecification` im Schema |
| 6 | **Google-Unternehmensprofil**: existiert eines? Ist `GOOGLE_REVIEW_URL` in Railway gesetzt, und auf welches Profil? | Auf der Startseite steht ein Knopf „Bei Google bewerten"; ob dahinter ein Profil liegt, ist nicht dokumentiert. Das Profil muss auf die Betreiberin laufen. **Der Standardwert im Code ist kein Profil, sondern ein Maps-Suchlink** (`mainweb/settings.py:409`, `https://www.google.com/maps/search/Luviq+Universe+Alsfeld`, geprüft 12.09.2026) — als `sameAs` wäre er eine Behauptung über eine Entität, die die Seite nicht belegen kann | Profiladresse in `sameAs` (`GE11`), Local SEO wird messbar |
| 7 | **Search Console**: Property `sc-domain:luviq-alsfeld.com` auf einem Konto der Betreiberin anlegen, Sitemap einreichen, Bastian als Nutzer aufnehmen | `GOOGLE_SEO_GUIDE.md` Schritt 1 — seit Mai 2026 offen; ohne Property ist die Sichtbarkeit nicht gemessen | `gsc_property` in `sites.json`, Klick- und Positionsdaten im Werkzeug |
| 8 | **Produktnamen**: zwei aktive Produkte heissen beide „Custom print hoodie" | `IS03`, `IS10`, `IS23`, `BF21`: gleicher Titel, geteilte Beschreibung, drei Seiten auf demselben Begriff | Eindeutige Namen im Shop-Admin; Titel und Slugs folgen automatisch |
| 9 | **Bewertungszahl**: ist „5.0 ★★★★★" belegt? | Wurde deshalb bewusst nicht ins Schema übernommen (`KV09`) | Erst mit echter Quelle ein `AggregateRating` |
| 10 | **Freigabe für zwei bekannte Lücken**: `comment_delete`, `admin_produkt_toggle`, `admin_resend_newsletter` und `admin_newsletter_reset` reagieren auf GET; eine E-Mail-Adresse kann sich zweimal registrieren | Beides ist durch Tests als Ist-Zustand festgehalten; die Änderung berührt Abläufe, die die Betreiberin täglich benutzt | Umstellung auf POST bzw. Eindeutigkeitsprüfung |
| 11 | **Sichtprüfung des Fokusrings** im Browser (Auflage 2 der Gegenprüfung) | Geschmacksentscheidung, keine Messfrage | Anpassung oder Bestätigung |
| 12 | **Dedup-Fenster der Besuchszählung**: 5 oder 30 Minuten? | Der Kommentar im Code nannte bis `93b3cde` fälschlich 30 Minuten; der Ist-Zustand sind 5 (`middleware.py`) | Wert bestätigen oder ändern |
| 13 | **Rücksendung: wer trägt das Rückporto, und binnen welcher Frist wird zurückgezahlt?** (neu mit SU04) | Weder AGB noch Datenschutzerklärung noch der Code regeln das. `/wissen/widerruf-und-ruecksendung/` sagt deshalb ausdrücklich, dass die Seite dazu nichts festlegt, statt eine Zahl zu nennen — eine erfundene Angabe wäre hier eine Zusage an den Käufer | Satz im Beitrag ersetzen, Aufnahme in die AGB und in `/wissen/` prüfen |
| 14 | **Eine eigene Beschreibung je Einzelstück** — welches Basisteil, welches Motiv, welche Grösse, welcher Zustand (neu mit IS19) | Der Zuwachs aus IS19 ist auf allen fünf Produktseiten wortgleich; er stammt aus AGB, Datenschutzerklärung und dem Code des Bestellvorgangs und beschreibt deshalb den **Kauf**, nicht das **Stück**. Was ein einzelnes Teil ausmacht, steht nirgends im Projekt und lässt sich nicht aus dem Code ableiten | `SU06`, `IS18`, `IS21`: eigener Text je Produkt im Shop-Admin (`Produkt.beschreibung`); die Produktseite gibt ihn ungeändert aus (`produkt_detail.html:130`). Seit `IS18` (11.09.2026, `2591fde`) hängen Startseite, Produktübersicht und Wissensübersicht nicht mehr daran — nur noch die fünf Produktseiten |
| 15 | **Profiladressen für `sameAs`** — welche Profile gehören zu Luviq Universe ausser Instagram (Google-Unternehmensprofil aus Nr. 6, TikTok, Branchenverzeichnisse)? (neu mit `GE11`, Paket 171) | `GE11` wurde am 11.09.2026 als **nicht möglich** beendet und am **12.09.2026 (Paket 183) zum zweiten Mal**, mit demselben Befund: `sameAs` in `templates/base.html:118` nennt allein `https://www.instagram.com/luviq.universe/` — dieselbe und einzige Adresse steht in `llms.txt` (`views/legal.py:166`). Der Bewertungsknopf zeigt im Standard auf einen Maps-**Suchlink** (`settings.py:409`), nicht auf ein Profil; TikTok erwähnt `GOOGLE_SEO_GUIDE.md` nur als Bio-Verlinkung, ohne Adresse. Eine Adresse zu raten hiesse, das Schema mit einem fremden Profil zu verknüpfen; es wurde in beiden Paketen keine Zeile Code geändert. Seit dem 12.09.2026 steht `GE11` dafür als **beim Kunden** im Bewertungsblock | Adressen in `sameAs` (`base.html`), Instagram-Zeile in `llms.txt` (`views/legal.py`) um dieselben Profile ergänzen, NAP-Tabelle in [50-LOCAL-SEO.md](50-LOCAL-SEO.md) nachziehen |
| 16 | **Stoff für eine zweite Seite in den einseitigen Bereichen** — was gehört neben `/ueber_uns/` (Werkstatt, Arbeitsweise, Material), neben `/liefergebiet/` (Orte, Abholung, Versandwege) und neben `/gaestebuch/`? (neu mit `SU08`, Paket 196) | `SU08` verlangt mehr als eine Seite je Themenbereich; vier Bereiche haben genau eine. Aus dem Projekt lässt sich keine zweite ableiten: `Produkt` hat kein Kategoriefeld (`shop1/models.py`), aus dem Kategorieseiten entstehen könnten, und `shop1/seiten_stand.py` führt genau 16 Seiten. `/produkte/` ist kein eigener Fall, sondern die Übersicht zu `/produkt/<slug>/` — zusammenlegen hiesse, indexierte Produktadressen umzuziehen. Am 12.09.2026 geprüft, keine Zeile Code geändert | Je gelieferter Angabe eine belegte zweite Seite im Bereich; trifft `SU08`, `SU01` und `SU02` |

## Erledigt

| Datum | Was | Beleg |
|---|---|---|
| 12.09.2026 | `SI17` und `PJ01` auf dem Zweig `sofort/2026-09-12-si17-und-2-weitere` — zwei Commits, **noch nicht gemergt**, **kein Template-Aufbau berührt**. *`SI17`:* die drei fremden Skripte im ausgelieferten HTML (`@alpinejs/intersect@3.14.8` und `alpinejs@3.14.8` in `templates/base.html`, `gsap@3.12.5` in `index.html`) und zusätzlich das vierte, das die Messung nicht sieht, weil es erst im Browser entsteht (`three@0.158.0`, per `document.createElement('script')` nachgeladen), tragen jetzt `integrity="sha256-…"` und `crossorigin="anonymous"`; beim nachgeladenen Three.js stehen `s.integrity` und `s.crossOrigin` **vor** `s.src`, weil erst `appendChild` den Abruf auslöst. Die Hashes stammen aus der Dateiauskunft von jsDelivr (Feld `hash` = SHA-256, base64), **je zweimal über verschiedene Endpunkte abgefragt** und mit einer Gegenprobe belegt: für `jquery@3.7.1` meldet dieselbe Auskunft genau die Zeichenfolge, die jQuery selbst als `integrity` veröffentlicht — der Lauf durfte weder `curl` noch `urllib` benutzen, und ein falscher Hash wäre schlimmer als keiner (Alpine lädt dann gar nicht mehr, und kein lokaler Test bemerkt es). Nur zwei Attribute an bestehenden `<script>`-Tags: keine Klasse, keine Kennung, kein Element, keine Reihenfolge geändert; die Designwache erfasst weder Attribute noch den `<head>`. **Offener Rest:** die zwei Chart.js-Einbindungen des Admin-Panels (siehe „Fehlt → Sicherheit"). *`PJ01`:* zweiter eigener Prüfbefehl `shop1/management/commands/pruefe_links.py`. `pruefe_seite` ruft die Adressen der Sitemap ab — ein Verweis **innerhalb** einer dieser Seiten steht in keiner Liste und fällt niemandem auf, wenn er ins Leere führt. Der neue Befehl liest `/`, jede Sitemap- **und jede llms.txt-Adresse** (letztere prüfte bisher niemand, obwohl sie von Hand geschrieben ist) und ruft jeden `<a href>` darin ab: eigene Adressen ab 400 sind Fehler, Weiterleitungen Warnungen — **ausser auf `LOGIN_URL`**, dort ist die Weiterleitung der Schutz selbst und stünde sonst in jedem Lauf; fremde Ziele werden nur auf `https` geprüft und **nicht** abgerufen. Verändernde Pfade (Warenkorb, Abmeldung, Werbeklick, Kommentar, Bestätigungsschlüssel, PayPal-Buchung) stehen mit Grund in `KEINE_PRUEFUNG`. Rahmen wie `pruefe_seite`: Testclient mit `secure=True`, Besuchsprotokoll aus, zurückgerollte Transaktion in beiden Datenbanken; Exitcode 1 bei Fehlern, `--streng` auch bei Warnungen. Eine Stelle in `pruefe_seite` mitgeändert: `_pruefhost` ist jetzt die Modulfunktion `pruefhost()`, damit beide Befehle denselben Host wählen. Sechs neue Tests (`VerweisPruefbefehlTest` in `test_einstellungen`), darunter die Probe, dass eine erfundene Route als Fehler herauskommt; **230/230 Tests grün laut Commit**. **Nicht an `start.sh` angeschlossen** — der Befehl gehört vor den Commit, nicht in den Containerstart. — `SU08` im selben Paket **nicht möglich** (siehe „Beim Kunden" Nr. 16 und den Bewertungsblock), keine Zeile Code dafür geändert | `680a641`, `11acccc` |
| 12.09.2026 | `VL21` (**anders eingebaut**) auf dem Zweig `sofort/2026-09-12-vl21-und-2-weitere` — ein Commit, **noch nicht gemergt** (siehe „Offen" Nr. 1). Die Karte, mit der dieser Shop sein Angebot zeigt, stand zweimal mitten in einer Vorlage (`produkte.html` 72–115, `index.html` 486–507); sie liegt jetzt im neuen Ordner `shop1/templates/shop1/teile/` und wird von dort per `{% include %}` in die bestehenden Schleifen eingebunden: `teile/angebot.html` aus `produkte.html` (`produkte_liste`, Bild 800×1000 mit `srcset`, Abzeichen „Unique"/„Limited", Label „Endpreis"), `teile/angebot_galerie.html` aus `index.html` (`produkte_galerie`, Bild 600×750 ohne `srcset`, Abzeichen „1 of 1"). **Zwei Bausteine statt des einen, den die Regel vorschlägt:** die beiden Karten unterscheiden sich in Ecken (`rounded-[3rem]` gegen `rounded-[3.5rem]`), Bildhöhe, Bildgrösse, `srcset`, Abzeichen und Aufbau des Textteils — ein gemeinsamer Baustein hätte entweder das Aussehen einer Ansicht geändert (Designwache) oder sechs Verzweigungen gebraucht; begründet in `LOGBUCH.md`, Paket 183. **Aussehen:** gerendertes HTML wörtlich unverändert (Reihenfolge, Klassen, Kennungen, Attribute aus den bisherigen Zeilen), `test_aufbau` unverändert grün, kein Tailwind-Neubau nötig (keine neue Klasse; `tailwind.config.js` scannt `shop1/templates/**/*.html` samt Unterordner). **Falle, festgehalten als Kommentar in beiden Dateien:** `{% load %}` gilt je Vorlagendatei und wird an einen Baustein nicht vererbt — ohne `{% load custom_tags %}` fehlt der Filter `cloud`, und Startseite wie Produktübersicht antworten mit 500. **Kein Test ergänzt** (die Designwache prüft genau das Ergebnis dieser Änderung), `seiten_stand.py` nicht angefasst (kein Inhalt geändert), kein Eintrag im Bewertungsblock. `manage.py check` ohne Befund, 224/224 Tests grün laut Commit. — `GE11` im selben Paket **nicht möglich** (zweites Mal, siehe „Beim Kunden" Nr. 15 und den Bewertungsblock), `RE07` **nicht möglich in diesem Lauf** (siehe „Offen" Nr. 3) | `5f517de` |
| 11.09.2026 | `KV07` auf dem Zweig `sofort/2026-09-11-kv07-und-2-weitere` — ein Commit, **seit dem Merge `c522ff9` in `main` und `origin/main` enthalten** (beide `4f91ac9`, lokale Referenz geprüft 12.09.2026; ob Railway diesen Stand ausgeliefert hat, ist nicht geprüft; live abgeschickt wurde das Formular noch nicht, siehe „Offen" Nr. 2 e). `views/shop.py::kontakt` leitet nach dem Start des Versands mit 302 auf die neue Route `kontakt_danke` (`/kontakt/danke/`, in `views/__init__.py` re-exportiert); die Erfolgsmeldung auf `/kontakt/` entfällt, die Fehlermeldungen (leere Felder, Versand scheitert beim Start) bleiben dort. Ein Neuladen schickt die Anfrage damit nicht mehr ein zweites Mal ab. Die Vorlage `kontakt_danke.html` trägt `noindex, follow`, steht weder in der Sitemap noch in `llms.txt`, kommt mit `never_cache` (jeder Aufruf erreicht den Server und das Besuchsprotokoll) und zeigt nichts aus der Anfrage — deshalb auch direkt abrufbar. Text nur mit Angaben, die schon auf `kontakt.html` stehen (Antwort per E-Mail, keine Telefonnummer, `brehlerluisa@gmail.com`), dazu Verweise auf die drei freigegebenen Wissensbeiträge; keine Antwortzeit, weil im Projekt keine steht. **Aussehen:** keine bestehende Seite verändert; die neue Seite folgt `payment_success.html` mit vorhandenen Klassen, Häkchen als Inline-SVG statt Emoji, `aufbau_referenz.json` **gezielt ergänzt** (217 Zeilen `+`, 0 `−`), `/kontakt/danke/` in `OEFFENTLICHE_SEITEN`, nicht in `INHALTSSEITEN`. **Tests** (`test_formulare`, 10 → 16): Weiterleitung und genau ein Versand, Bestätigung auf eigener Adresse, gescheiterter Versand bleibt auf `/kontakt/`, neue Klasse `KontaktDankeTest` (direkt abrufbar ohne Anfragedaten, `noindex` und nicht in Sitemap und `llms.txt`, `no-store`, nur die Adresse der Kontaktseite und kein `tel:`). `manage.py check` grün, 224/224 Tests grün laut Commit. **Nicht in `seiten_stand.py`** — der `WebPage`-Knoten der Seite bleibt damit ohne `name` und `dateModified`. **Offen:** `EIG10` — der Versand läuft weiter im Thread, ein späterer Zustellfehler landet nur im Protokoll, die Danke-Seite erscheint trotzdem; sie sagt deshalb „abgeschickt", nicht „zugestellt". Kein Eintrag im Bewertungsblock | `2d78a55` |
| 11.09.2026 | `VL01` als **Ausnahme „bewusst so"** im Bewertungsblock begründet — kein Umbau auf `config/` + `apps/`; Belege: Einstieg `mainweb` in `manage.py`, `wsgi.py`, `settings.py` und `start.sh`, App-Name `shop1` an 18 Migrationen und den mit PyStore geteilten Tabellen, acht nicht gemergte `sofort/*`-Zweige mit Änderungen unter `shop1/` oder `mainweb/`. Nur diese Datei zwischen den Bewertungsmarken geändert. Zweig `sofort/2026-09-11-kv07-und-2-weitere`, seit `c522ff9` in `main`. — `GE11` im selben Paket **nicht möglich**, siehe „Beim Kunden" Nr. 15 | `7919341` |
| 11.09.2026 | `IS18` (**anders eingebaut**) auf dem Zweig `sofort/2026-09-11-is18` — ein Commit, seit dem Merge `1a5f36b` (mit der Nachbesserung `54104f6`) in `main` und `origin/main` enthalten (lokale Referenz; Auslieferung durch Railway nicht geprüft). Paket 162 hatte den ganzen Punkt als „beim Kunden" geführt; die Gegenprüfung lehnte das ab, weil die Begründung nur für die fünf Produktseiten trägt. Gebaut ist deshalb nur der Teil, der nicht an der Beschreibung je Stück hängt: `/` (die drei Feature-Absätze, der Absatz von Luisa Brehler, der Newsletter-Absatz und der Anmeldehinweis im Kommentarbereich von `_reviews_map.html`), `/produkte/` (Einleitungsabsatz) und `/wissen/` (Einleitungsabsatz und die zwei Absätze unter „Woher die Angaben stammen"). Inhalt: Unregelmäßigkeiten nach § 3 AGB, eine Größe je Basisteil, Kauf eines vorhandenen Stücks statt Auftrag, Bestellablauf, PayPal-Prüfung auf dem Server, Bankdaten per E-Mail, Abschalten eines Stücks nach der Zahlung (PayPal `checkout.py:231`, Vorab-Überweisung erst mit Eintrag „bezahlt" im Panel `admin_views.py:746-751`), keine Reservierung durch den Warenkorb, Eigentumsvorbehalt, Widerruf ohne Formvorgabe mit Rücksendeanschrift, Rückporto ausdrücklich ungeregelt, Registrierungs- und Pflichtfelder, Anmeldesperre, Kontaktfelder, Datenschutz-Dienste — jede Angabe mit Fundstelle in `LOGBUCH.md` (Paket 165). **Nur Text in bestehenden Absätzen**: kein Element, keine Klasse, keine Kennung, keine Überschrift, `test_aufbau` unverändert grün. Laut Commit erreichen die drei Seiten ihre Zielgrösse (700 / 600 / 900) ohne ein Produkt im Bestand; `MINDESTWOERTER` nachgezogen, `seiten_stand.py` für `home`, `produkte` und `wissen` auf `2026-09-11`. `manage.py check` grün, 218/218 Tests grün laut Commit. **Offen:** die fünf Produktseiten („Beim Kunden" Nr. 14), die Live-Nachmessung („Offen" Nr. 2 d). Kein Eintrag im Bewertungsblock. **Nebenbefund, nicht angefasst:** der Einleitungsabsatz von `/produkte/` steht in Versalien mit weiter Laufweite und ist bei diesem Umfang lang zu lesen — eine andere Satzart wäre eine Änderung am Aussehen und braucht eine Freigabe ([20-DESIGN.md](20-DESIGN.md)) | `2591fde` |
| 11.09.2026 | `SI09` auf dem Zweig `sofort/2026-09-11-si09` — ein Commit, seit dem Merge `4ec540b` in `main` und `origin/main` enthalten (lokale Referenz; Auslieferung durch Railway nicht geprüft). `ContentSecurityPolicyMiddleware` erzeugt je Anfrage eine Nonce (`secrets.token_urlsafe(18)`, `request.csp_nonce`) und hängt sie als `'nonce-…'` an `script-src`; `'unsafe-inline'` ist aus `CSP_QUELLEN['script-src']` gestrichen. Der neue Kontextprozessor `csp_nonce` reicht sie an die Vorlagen: jedes Inline-Skript trägt `nonce="{{ csp_nonce }}"` (`base.html`, `index.html`, `payment.html`, `admin/stats.html`, `admin/werbung_list.html`), das PayPal-SDK zusätzlich `data-csp-nonce`. Die Handler-Attribute (`onclick`, `onsubmit`, `onchange`, `onerror`, `onload`), die keine Nonce deckt, sind ersetzt durch `data-bestaetigen`, `data-bei-fehler-ausblenden`, `data-auto-absenden`, `data-schrift-nachladen` (ein Skript im `<head>` von `base.html`) und `data-werbung` (Seitenskript der Kampagnenliste). Kein Element, keine Klasse, keine Kennung verändert. Drei neue Tests in `test_einstellungen`: Nonce je Antwort neu und ohne `'unsafe-inline'`, jedes Inline-Skript jeder öffentlichen und jeder Panel-Seite mit der Nonce seiner Antwort, keine Vorlage mit `on…=`, `javascript:` oder Skript ohne Nonce. Wortgleicher Nachbau von `4f3ac5f` (Paket 162, dort nicht ausgeliefert). `manage.py check` grün, 218/218 Tests grün laut Commit. **Nicht belegt:** das Verhalten der Ersatzskripte im Browser. **Bewusst offen:** `'unsafe-eval'` und `'unsafe-inline'` im `style-src` (siehe „Fehlt → Sicherheit") | `28d1ca3` |
| 11.09.2026 | `PJ05`, `SI08`, `PJ11` auf dem Zweig `sofort/2026-09-11-pj05-und-2-weitere` — drei Punkte, je ein Commit, **kein Template, kein Aufbau berührt**; seit dem Merge `0c18ea7` in `main` und `origin/main` enthalten, die Auslieferung durch Railway ist hier nicht geprüft. *`PJ05`* (anders eingebaut): derselbe Code wie `e36f4bc` — verschluckte Ausnahme protokolliert, `Subscriber` im Django-Admin, sechs bewusst offene Stellen mit `# offen-ok:` bzw. `# audit-ok K02:` begründet; Einzelheiten unter „Verbesserungsmöglichkeiten → Code und Projektgerüst". *`SI08`:* `CSP_MODUS` hat die Vorgabe `scharf`, `report-only` bleibt der Rückweg ohne neuen Stand. Anders als in `a1169a4` folgt die Positivliste für PayPal jetzt PayPals eigener Angabe zum JS-SDK (developer.paypal.com/sdk/js/csp/): `*.paypal.com`, `*.paypalobjects.com`, `*.venmo.com` in `script-src`, `style-src`, `connect-src` und `frame-src` — `style-src` fehlte bisher ganz, `*.paypalobjects.com` in `frame-src`; `form-action` erlaubt vorsorglich `*.paypal.com`. Die Tests halten die Vorgabe-Kopfzeile, den Rückweg und die PayPal-Einträge gegen PayPals Angabe; `pruefe_seite` warnt im Auslieferungsstand nicht mehr. Nebenbefund, nicht angefasst: `SECURE_CROSS_ORIGIN_OPENER_POLICY` ist nicht gesetzt, Django sendet `same-origin`, PayPal empfiehlt `same-origin-allow-popups`. *`PJ11`:* `==` in jeder Zeile, `requirements.lock` per `--constraint`, Django 5.2.17 statt der Spanne `>=5.0,<6.1`. Prüfbefehl `manage.py check` grün, 215/215 Tests grün laut Commits; die Suite lief dabei mit Django 6.0.5 | `9ebad08`, `9a3226f`, `51cbf74` |
| 08.09.2026 | **Nicht ausgeliefert:** die drei Commits liegen allein auf `sofort/2026-09-08-pj05-und-2-weitere` (`git branch --contains`, geprüft 11.09.2026), nicht auf `main`. `PJ05` und `SI08` sind am 11.09.2026 neu gebaut (Zeile darüber), `TS11` nicht. — `PJ05`, `TS11`, `SI08` — drei Punkte, je ein Commit, **ohne eine Änderung an Template, Aufbau oder Klasse**. *`SI08`:* Vorgabe von `CSP_MODUS` ist `scharf` statt `report-only` — der Browser blockiert jetzt, was nicht in `CSP_QUELLEN` steht, statt es nur in der Konsole zu melden. An die Stelle der geplanten Browserprüfung tritt der Beleg aus der Testsuite (die beiden Deckungstests halten jedes `<script src>`, jedes Stylesheet, jedes Iframe und das PayPal-SDK jeder öffentlichen Seite und jeder Panel-Seite gegen die Liste). Eine Direktive wurde dabei weiter: `form-action` erlaubt neben `'self'` jetzt `https://*.paypal.com`, weil das SDK den Bezahlvorgang notfalls per Formular in ein neues Fenster schickt. Rückweg ohne neuen Stand: `CSP_MODUS=report-only`. *`TS11`:* `CANONICAL_HOST` hat die Hauptadresse `www.luviq-alsfeld.com` als Vorgabe statt einer leeren Zeichenkette — die Variable war in Railway nie gesetzt, deshalb lief die 301 seit Schritt 37 nie. Die Umgebungsvariable behält den Vorrang, ein leerer Wert schaltet die Weiterleitung weiter ab. *`PJ05`:* siehe „Verbesserungsmöglichkeiten → Code und Projektgerüst". Prüfbefehl grün, 216/216 Tests grün, `test_aufbau` unverändert | `e36f4bc`, `ecc84f2`, `a1169a4` |
| 08.09.2026 | `GE23`, `GE25`, `IS19` — drei Textpunkte, je ein Commit, **ohne eine Änderung am Aufbau** (kein neues Element, keine geänderte Klasse, keine Kennung; der Zuwachs steht in Absätzen, die es schon gab). *Antwort zuerst:* jeder erste Absatz nennt im ersten Satz, was die Seite ist, und trägt eine belegte Zahl — `/produkte/` und `/gaestebuch/` die Bestandszahl aus der Datenbank (`produkte_liste\|length`, `comments\|length`), `/kontakt/`, `/ueber_uns/` und `/liefergebiet/` die PLZ 36304 und die Versanddauer, `/datenschutz/` seine vier Abschnitte, `/agb/` seine fünf Paragraphen und die 14-Tage-Frist, jede Produktseite Name, Preis und Herkunft aus dem Datensatz; die Ausnahmemenge `OHNE_ZAHL_IM_ERSTEN_DRITTEL` ist damit leer. *Zahlen:* nur Angaben mit Beleg im Projekt (14 Tage aus § 5 AGB, Versand 1–2 und Zustellung 1–3 Werktage, 5 Minuten Sperrfrist und ein Besuch je Sitzung und Tag aus `middleware.py`, `SESSION_COOKIE_AGE = 1209600` = 14 Tage). *Dünne Seiten:* keine indexierbare Seite unter 200 Wörtern im Inhaltsbereich — gemessen mit einem Produkt und ohne Kommentare `/produkte/` 111 → 238, `/kontakt/` 135 → 245, `/gaestebuch/` 99 → 300, Produktseite 103 → 211, `/agb/` 177 → 240; `MINDESTWOERTER` nachgezogen, 215/215 Tests grün, `test_aufbau` unverändert | `fd82efd`, `2b26108`, `b35f6e4` |
| 07.09.2026 | `SU04`: drei Wissensbeiträge ohne Freigabevorbehalt — Bestellen und Bezahlen (1.129 W), Widerruf und Rücksendung (1.034 W), Konto und Daten (1.000 W); jede Sachangabe aus AGB, Datenschutz, Impressum, Liefergebiet oder dem Shop-Code belegt. Angemeldet in `WISSEN_BEITRAEGE`, `seiten_stand.py` (Stand und Name), `WISSEN_SEITEN`, `tests/_basis.py`, `FAQ_SEITEN`, `MINDESTWOERTER` und der Designwache-Referenz (gezielt ergänzt); `/wissen/` damit indexierbar, kein Designwechsel, 215/215 Tests grün | `d7d2e0b` |
| 02.09.2026 | Auflage 5: Logbuch-Einträge für die Schritte 1–10 nachgetragen, `CLAUDE.md` auf den heutigen Code gezogen (14 Testmodule, Middlewares, Wissensbereich, Prüfbefehl, Gunicorn) | `511ffe5` |
| 02.09.2026 | Auflage 3: Wissensbeiträge mit Freigabeschalter aus dem Index genommen (`noindex`, nicht in Sitemap und llms.txt), `WissensfreigabeTest` | `60555d0` |
| 02.09.2026 | Auflage 1: acht zusammengeklebte Attribute in `checkout.html` repariert, `AttributSyntaxTest` prüft den Rohtext jedes Start-Tags | `b750337` |
| 02.09.2026 | Auflage 4: `test_geo` vergleicht das Änderungsdatum in der Zeitzone der Seite — die Suite ist nicht mehr täglich zwischen 22 und 24 Uhr UTC rot | `270c5f9` |
| 02.09.2026 | Welle 9: Warenkorb-, Zahlungs-, Konto-, Zugriffsschutz- und Invarianten-Tests (174 → 215) | `87a4e85`, `bbdd58e`, `b776c6e`, `3d73147`, `2e9b051` |
| 02.09.2026 | Welle 8: CSP-Middleware (Report-Only), `CanonicalHostMiddleware` (301), `pruefe_seite` auf die ausgelieferte Seite ausgeweitet und in `start.sh` angeschlossen, Betriebs- und Schutztests | `3a9c2d3`, `6bd5eb4`, `50d68da`, `0bcd0ef` |
| 01.09.2026 | Welle 7: totes Hintergrundbild entfernt, WebP in mehreren Breiten (498,6 → 175,3 KB), GZip, Sitemap- und llms-Cache, `CACHES` gesetzt, Warenkorb-Zähler in einer Abfrage, Geo-Pool, Gunicorn `gthread` | `64055fe` … `b0fba20` |
| 01.09.2026 | Welle 6: Wissensbereich angelegt und angeschlossen — Register, Routen, Übersicht, drei Beiträge (821 / 941 / 890 Wörter) | Schritte 26–30 (`74fba7a`) |
| 01.09.2026 | Welle 5: Antwort-zuerst-Texte auf Startseite, `/produkte/`, `/ueber_uns/`, `/gaestebuch/`, Produktseiten und Impressum; Inhaltstests messen Wortzahlen statt zu schätzen | Schritte 21–25 |
| 01.09.2026 | Welle 4: `WebPage`-Knoten mit gepflegtem `dateModified`, `Person`-Knoten `#luisa`, `BreadcrumbList` zentral, `llms.txt` mit Ort, Versandzeiten und Instagram, GEO-Tests 16 → 20 | Schritte 16–20 |
| 01.09.2026 | Welle 3: Meta-Beschreibungen 157–171 Zeichen mit Aufforderung, Ortsbezug im Produkttitel, Produkt-Metaangaben auf 60/160 begrenzt, `lastmod`-Register, `/produkt/` → 301 | Schritte 11–15 |
| 01.09.2026 | Welle 2: jedes Formularfeld mit zugänglichem Namen, Symbol-Links benannt, Alternativtexte als Beschreibungen, Tastaturfokus sichtbar | Schritte 6–10 |
| 01.09.2026 | Welle 1: `pruefe_seite` unter Versionskontrolle, vertauschte Rechte-Beschriftungen Staff/Admin richtiggestellt, acht verschluckte Ausnahmen protokolliert, tote Importe entfernt, fünf schwache Tests scharf gestellt | Schritte 1–5 |
| 01.09.2026 | Lauf 3: toter Code entfernt, Doku an den Code angeglichen, `LOGBUCH.md` angelegt | `c6e0cce`, `342f7fd` |
| 01.09.2026 | `GOOGLE_SEO_GUIDE.md`: falscher Zielbegriff „railway hosting luviq" gestrichen, DEBUG-Abschnitt richtiggestellt | `691b866` |
| 18.08.2026 | README im Repo; letzte Railway-Auslieferung (SUCCESS) | `645842b` |
| 15.07.2026 | Startseite PageSpeed und Politur; `CLAUDE.md` versioniert — **letzter Stand auf `main`** | `2fe90a0`, `2a17edd` |
| 13.07.2026 | Admin-Mail pro Seitenbesuch vollständig entfernt (Mail-Flut durch Bots ohne Cookies) | `e58775a`, `26b6e72` |
| 05.07.2026 | Bot-Filter geschärft, Hero-Showcase überarbeitet | `8b854c2` |
| 03.07.2026 | Hero der Startseite mit Produktfoto, Trust-Badges und Ticker | `82f3077`, `67fb4fb` |
| 25.05.2026 | Grundausstattung SEO: dynamische Sitemap, robots.txt, Meta und Open Graph, Schema `Organization`/`ClothingStore`/`WebSite`/`ItemList`/`Product` | `GOOGLE_SEO_GUIDE.md`, `DOCUMENTATION.md` |

## Bewertung der Messpunkte

<!-- bewertung:anfang -->
| Punkt | Zustand | Grund | seit |
|---|---|---|---|
| SU09 | beim Kunden | `/produkt/` leitet seit Welle 3 per 301 auf die Übersicht `/produkte/` (`shop1/views/legal.py::produkt_uebersicht_redirect`, Test `test_seo.test_produkt_ohne_kennung_leitet_auf_die_uebersicht`); gemessen wurde die Live-Seite, auf der der Zweig noch nicht ausgeliefert ist — eine zweite Übersichtsseite unter `/produkt/` wäre ein Volltext-Duplikat von `/produkte/` und träfe den Befund IS21. | 2026-09-05 |
| SU02 | beim Kunden | Von 9.350 fehlenden Eigenwörtern liegen 2.913 fertig, aber wegen `'freigegeben': False` auf `noindex`, und die restlichen rund 6.400 verlangen Preisrahmen, Abläufe und echte Fälle, die im Repository nirgends stehen — beides kann nur die Betreiberin liefern. | 2026-09-06 |
| SU07 | beim Kunden | Drei fertige Wissensbeiträge auf sechs Verkaufsseiten übertreffen die Zielgrösse bereits, stehen aber wegen unbelegter Pflege- und Grössenangaben auf `'freigegeben': False` (`shop1/views/wissen.py:53,62,71`) und damit auf `noindex` — nur die Betreiberin kann diese Angaben bestätigen. | 2026-09-07 |
| KV01 | beim Kunden | Im ganzen Projekt steht keine geschäftliche Telefonnummer; die frühere Platzhalternummer wurde entfernt und steht als verbotenes Ziel in `FALSCHE_ZIELE` (`shop1/tests/test_inhalt.py`). Eine Nummer zu erfinden hiesse, Kunden auf einen Anschluss zu schicken, der der Betreiberin nicht gehört — nur Luisa Brehler kann eine nennen (siehe „Beim Kunden" Nr. 4). Es wurde dafür keine Zeile geändert. | 2026-09-08 |
| IS21 | beim Kunden | Alle zehn gemeldeten Paare sind Produktseiten-Paare, und deren Text unterscheidet sich laut `produkt_detail.html:78,115,119,129,130` allein durch Name, Preis und Beschreibung — zwei aktive Stücke tragen beide identisch, und `models.py:85-99` bietet kein weiteres Feld, aus dem sich Unterschied gewinnen liesse. | 2026-09-08 |
| TS11 | beim Kunden | Die 301 bleibt gebaut (`mainweb/settings.py:56-57`, `shop1/middleware.py:32`, Test `shop1/tests/test_einstellungen.py:136`); neu ist der Eintrag im Bewertungsblock `doku/80-AUFGABEN.md:186` als **beim Kunden** mit Beleg — Codestelle, Live-Befund EIG19 und die Fundstelle der Messlücke; der Punkt bleibt damit in der Wertung | 2026-09-08 |
| SU01 | beim Kunden | Zur Zielgrösse von 30 rankfähigen Seiten fehlen Seiten, die sich nicht aus dem Projekt belegen lassen: die drei fertigen Wissensbeiträge stehen wegen unbelegter Pflege- und Grössenangaben auf `'freigegeben': False` (`shop1/views/wissen.py:63,72,81`) und damit auf `noindex`, und jede weitere Seite bräuchte Angaben zu Materialien, Preisrahmen oder Rückporto, die im Repository nirgends stehen („Beim Kunden" Nr. 1–3 und 13). Freigeben oder liefern kann das nur Luisa Brehler; in Paket 162 wurde dafür keine Zeile Code geändert und nichts erfunden. | 2026-09-11 |
| VL01 | bewusst so | „Bewusst so“ im Bewertungsblock: das Gerüst gehört zur Landingpage-Vorlage, `mainweb` und `shop1` stecken in Startbefehl, 18 Migrationen und geteilten Tabellen, und alle acht offenen Zweige würden kollidieren (`7919341`, nur Doku). | 2026-09-11 |
| GE11 | beim Kunden | Im ganzen Projekt steht genau eine Profiladresse: `https://www.instagram.com/luviq.universe/` (`templates/base.html:118` im `sameAs`, `shop1/views/legal.py:166` in `llms.txt`). `GOOGLE_REVIEW_URL` ist im Standard ein Maps-**Suchlink** (`mainweb/settings.py:409`), kein Profil, und TikTok nennt `GOOGLE_SEO_GUIDE.md` nur als Bio-Verlinkung ohne Adresse. Eine weitere Adresse in `sameAs` wäre eine Behauptung über eine Entität, die die Seite nicht belegen kann — nennen kann sie nur Luisa Brehler (siehe „Beim Kunden" Nr. 6 und 15). In den Paketen 171 und 183 wurde dafür keine Zeile Code geändert. | 2026-09-12 |
| BF08 | bewusst so | Eine Sprungmarke **muss** ein zusätzlicher Link an erster Stelle im Körper sein und verschiebt damit den Fingerabdruck jeder Seite. Genau das hat die Designwache des Tors am 12.09.2026 am Einbau `f37577b` beanstandet — fünf Abweichungen auf `/`, `/produkte/`, `/kontakt/`, `/impressum/`, `/datenschutz/`, je „295 → 296 Elemente, erste Abweichung an Stelle 1: `div#scroll-progress` wurde zu `a.sprungmarke`" — und die Rücknahme verlangt; sie ist in der Nachbesserung zu Paket 188 vollzogen (`templates/base.html`, `shop1/static/shop1/style.css`, `shop1/tests/aufbau_referenz.json`, `shop1/tests/test_barrierefreiheit.py` wieder wörtlich auf dem Stand vor `f37577b`). Der Körper beginnt damit bewusst weiter ohne Sprungmarke: Grenze 1 des Verbesserungslaufs (kein Eingriff in Aufbau, Reihenfolge, Klassen, Kennungen) schlägt den Punkt. Baubar wird er erst mit einer ausdrücklichen Freigabe für eine Aufbauänderung auf allen 23 bewachten Seiten — der Code dafür ist in `f37577b` fertig und lässt sich zurückholen. | 2026-09-12 |
| GE24 | bewusst so | Frage-Überschriften stehen bereits überall dort, wo diese Seite Fragen beantwortet: `/wissen/` (6 von 11 Überschriften) und die sechs Wissensbeiträge (7 bis 11 Fragen je Beitrag, alle `<h2>`), dazu `/kontakt/` („Wie lange dauert die Lieferung?", „Zahlungsmethoden?") und `/liefergebiet/` (3 Fragen unter „Fragen zu Versand & Region") — gemessen am 12.09.2026 über den Testclient auf allen 23 bewachten Seiten. Die Seiten ohne Frage-Überschrift sind Pflicht- und Funktionsseiten, deren Gliederung keine Frage verträgt: `/impressum/` („Angaben gemäß § 5 TMG"), `/agb/` („§ 1 Geltungsbereich" bis „§ 5 Widerrufsrecht") und `/datenschutz/` („1. Datenschutz auf einen Blick" bis „4. Ihre Rechte") folgen einer normativen Gliederung, `/login/`, `/register/`, `/kontakt/danke/` und `/gaestebuch/` tragen ausser den beiden Footer-Überschriften genau eine, und `/produkte/` wie die Produktdetailseite setzen Produktnamen aus der Datenbank als Überschrift. Bei `/`, `/produkte/` und `/ueber_uns/` sind die Überschriften („LUVIQ", „The Drop", „VOM VINTAGE ZUM MASTERPIECE") die in `doku/20-DESIGN.md` festgehaltenen „grossen schwarzen Versalien in der Display-Schrift" — sie umzuschreiben wäre eine sichtbare Änderung, und Überschriftentexte schützt Regel 1 des Verbesserungslaufs wörtlich (`shop1/tests/test_aufbau.py`). Es wurde dafür keine Zeile Code geändert. | 2026-09-12 |
| RE07 | beim Kunden | Die Umstellung auf selbst gehostete Schriften braucht die Schriftdateien, und die kann dieser Lauf nicht beschaffen: im ganzen Repository liegt keine einzige `*.woff2`/`*.ttf`/`*.otf`-Datei (Glob über alle Ordner, leer, geprüft 12.09.2026), und weder `curl` noch `urllib` noch ein anderer Netzzugang war in den Paketen 183 und 188 freigegeben — `templates/base.html:212-215` lädt Inter (400–700) und Outfit (700/900) deshalb unverändert von `fonts.googleapis.com`. Ein Teilumbau bringt rechtlich nichts: Inter-Schnitte liegen in einem Nachbarprojekt desselben Rechners (`RTC-Service/static/fonts/inter-latin.woff2`), Outfit in keinem — hostete man nur Inter selbst, ginge die Adresse des Besuchers wegen Outfit trotzdem zu Google, und Outfit zu ersetzen wäre eine Änderung an der Display-Schrift ([20-DESIGN.md](20-DESIGN.md)). Jemand mit Netzzugang muss die sechs Schnitte einmal beschaffen und nach `shop1/static/shop1/` legen; danach ist der Punkt in einer halben Stunde gebaut. In den Paketen 183 und 188 wurde dafür keine Zeile Code geändert. | 2026-09-12 |
| SU08 | beim Kunden | Einseitig sind vier Bereiche: `/produkte/`, `/gaestebuch/`, `/ueber_uns/`, `/liefergebiet/`. `/produkte/` ist kein Silo ohne Unterseiten, sondern die **Übersicht** des Produktbereichs, dessen Detailseiten unter `/produkt/<slug>/` liegen (`shop1/urls.py`); beide zusammenzulegen hiesse, jede indexierte Produktadresse der laufenden Seite umzuziehen — ein Eingriff in die kanonischen Adressen, der weit über einen Strukturpunkt hinausgeht. Für `/gaestebuch/`, `/ueber_uns/` und `/liefergebiet/` gibt es den Inhalt einer zweiten Seite im Projekt nicht: `Produkt` hat **kein Kategoriefeld** (`shop1/models.py`), aus dem sich Unterseiten ableiten liessen, und das Seitenregister `shop1/seiten_stand.py` führt genau 16 Seiten. Orte, Lieferzeiten, Arbeitsweise oder eine zweite Werkstattseite kann nur Luisa Brehler liefern (siehe „Beim Kunden" Nr. 16); sie zu erfinden ist ausgeschlossen. In Paket 196 wurde dafür keine Zeile Code geändert. | 2026-09-12 |
<!-- bewertung:ende -->

## Eigene Punkte

<!-- eigenepunkte:anfang -->
| Punkt | Titel | Bereich | Zustand | Beleg | seit |
|---|---|---|---|---|---|
| EIG01 | Eine gelöschte Datenvariable steht noch in llms.txt und llms-full.txt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG02 | Startseite und Über-mich melden lastmod 04.09., obwohl sie am 06./07.09. geändert wurden | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG03 | Die Inhalte-Doku kennt vier Ratgebertexte nicht und widerspricht ihrem eigenen Kopf | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG04 | Ratgebertexte melden og:type „website", das Schema nennt sie Article | seo | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG05 | Impressum zitiert drei aufgehobene Rechtsgrundlagen | recht | offen | bei einer anderen betreuten Seite aufgefallen am 07.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-07 |
| EIG06 | Die Bewertung des eigenen Entwicklers steht sichtbar als Kundenstimme | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG07 | Das Schema baut seine Bild-URLs von Hand am Static-Manifest vorbei | seo | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG08 | Ein Kauf schaltet das Stück über seinen Namen ab — zwei Stücke heißen gleich | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: kritisch): shop1/views/checkout.py:226` bildet `{p.name: p …}`, `shop1/admin_views.py:748` nimmt `filter(name=…).first()`; live tragen `/produkt/custom-print-hoodie-1/` und `/produkt/custom-print-hoodie/` beide den Namen „Custom print hoodie" (llms.txt, 08.09.2026). Der Kauf des einen kann das andere deaktivieren und das verkaufte im Shop stehen lassen | 2026-09-08 |
| EIG09 | Vier von fünf Einzelstücken tragen „-ausverkauft" im Text und stehen trotzdem als kaufbar im Shop | inhalte | offen | Tiefenanalyse 08.09.2026 (Schwere: kritisch): llms.txt live: „Custom hoodie mit print: 59.00 EUR – Hoodie mit backprint -ausverkauft" (ebenso `custom-print-hoodie-1`, `custom-print-jacke`, `custom-print-hoodie`); `/produkte/` zeigt dieselben Stücke am 08.09.2026 als „Unique Limited" mit Preis, `/produkt/custom-print-hoodie/` zeigt „-ausverkauft" neben dem Kaufweg | 2026-09-08 |
| EIG10 | Das Kontaktformular meldet Erfolg, auch wenn die Mail nie hinausgeht | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: kritisch): shop1/views/shop.py:85-86` meldet „erfolgreich gesendet" im `try` um `send_brevo_email`, das in `shop1/utils.py:62` nur einen `threading.Thread` startet — jeder echte Sendefehler landet ausschließlich im Protokoll. Die Anfrage wird nirgends gespeichert (kein Modell dafür) | 2026-09-08 |
| EIG11 | Die Doku beschreibt einen Live-Stand, den es seit dem Deploy nicht mehr gibt | status | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): doku/00-STATUS.md:30` („Live-Stand `main` @ `2a17edd` (15.07.2026)") und `doku/90-NOTIZEN.md:36` („Die Live-Seite zeigt also main, nicht diesen Zweig") gegen live: sitemap.xml führt am 08.09.2026 `/wissen/`, `/wissen/bestellen-und-bezahlen/`, `/wissen/widerruf-und-ruecksendung/`, `/wissen/konto-und-daten/`; `/kontakt/` nennt „Grünberger Str. 16, 36304 Alsfeld" statt der in `doku/90-NOTIZEN.md:167` beschriebenen Platzhalter | 2026-09-08 |
| EIG12 | Die indexierte Wissens-Übersicht veröffentlicht genau die Angaben, für die die Freigabe fehlt | inhalte | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/views/wissen.py:61-66`: der Kurztext „Waschen auf links bei 30 °C, Trocknen an der Luft, Bügeln nur von links" steht bei `'freigegeben': False`. Live steht dieser Satz auf https://www.luviq-alsfeld.com/wissen/ — einer Seite, die in der Sitemap steht und indexiert wird. Das `noindex` des Beitrags schützt die unbelegte Angabe damit nicht | 2026-09-08 |
| EIG13 | Fünf Sterne ohne Bewertung: „5.0" fest verdrahtet, jeder Kommentar bekommt Sterne, zwei von drei aus eigenen Konten | inhalte | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/templates/shop1/_reviews_map.html:116-117` schreibt „5.0 ★★★★★" als festen Text, `:56` hängt an **jeden** Gästebuch-Kommentar ★★★★★, obwohl `Comment` kein Bewertungsfeld hat. Live auf `/gaestebuch/` (08.09.2026) drei Kommentare, davon „shopbesitzer" (09.05.2026) und „luisabre" (08.05.2026) | 2026-09-08 |
| EIG14 | Google-Maps-iframe lädt vor jeder Einwilligung — die Doku sagt, es werde nichts Einwilligungspflichtiges geladen | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/templates/shop1/_reviews_map.html:13-19` und `:97-103` binden `maps.google.com` auf `/` und `/gaestebuch/` ein; `doku/90-NOTIZEN.md:196`: „Kein Consent-Banner — heute richtig, weil nichts Einwilligungspflichtiges geladen wird". Der Messbefund bestätigt den Host live (RE06: `maps.google.com` eingebunden) | 2026-09-08 |
| EIG15 | Der Willkommensrabatt wird nur bei PayPal verbraucht | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/views/checkout.py:220` setzt `has_welcome_discount = False` ausschließlich in `paypal_capture`; der Zweig `bank_transfer` (`checkout.py:132-138`) geht direkt auf `payment_success`. Wer per Vorab-Überweisung zahlt, behält die 10 % für jede weitere Bestellung | 2026-09-08 |
| EIG16 | Die Bestellbestätigung nennt Einzelpreise, deren Summe nicht zum Gesamtbetrag passt | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/views/checkout.py:267-286`: die Mail listet `menge x produkt_preis` und darunter `order.gesamt_betrag`, der bereits um 10 % gemindert ist. Das Modell `Order` hat kein Rabattfeld; der Wert lebt nur in der Session (`checkout.py:120`), taucht also weder in der Mail noch in der Bestellansicht auf | 2026-09-08 |
| EIG17 | Newsletter ohne Bestätigungsschritt, ohne Adressprüfung, ohne Abmeldeweg | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): shop1/views/legal.py:351` legt mit `Subscriber.objects.create(email=email)` sofort einen Empfänger an — keine Bestätigungsmail, keine Validierung (`.create()` umgeht die `EmailField`-Prüfung). `shop1/utils.py:65-127` versendet an alle Abonnenten, ohne einen Abmeldelink zu enthalten; ausgelöst aus `shop1/admin_views.py:555 | 2026-09-08 |
| EIG18 | Bestätigungs- und Newsletter-Links hängen an `SITE_URL`; der Vorgabewert zeigt auf einen toten Host | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): mainweb/settings.py:275` fällt ohne die Variable auf `https://luviq-luisa-production.up.railway.app` zurück — dieser Host antwortete am 08.09.2026 mit HTTP 404 (dokumentiert ist der Dienst als `luviq-luisa-shop.up.railway.app`). `shop1/signals.py:48-49` baut daraus den Verifikationslink, `shop1/utils.py:68` die Newsletter-Links. Ohne bestätigtes Konto gibt es keinen Kauf; `pruefe_seite.py:204` warnt nur ins Log | 2026-09-08 |
| EIG19 | Die 301 auf `www` liegt live im Code — es fehlt allein die Umgebungsvariable | seo | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): CanonicalHostMiddleware` wirkt nur mit gesetztem `CANONICAL_HOST` (`mainweb/settings.py:44-48`). Der Zweig ist ausgeliefert (Sitemap führt `/wissen/`), trotzdem liefert `https://luviq-alsfeld.com/` am 08.09.2026 die Seite selbst statt einer Weiterleitung. TS11 ist damit kein Code-, sondern ein Railway-Punkt | 2026-09-08 |
| EIG20 | Die Search Console zählt drei Hostvarianten getrennt — die stärkste ist die ungesicherte Apex-Adresse | seo | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): GSC 09.08.–05.09.2026: `http://luviq-alsfeld.com/` 12 Klicks / 452 Impressionen, `https://www.luviq-alsfeld.com/` 4 / 102, dazu `https://luviq-alsfeld.com/produkte/` 2 / 20 gegen `https://www.luviq-alsfeld.com/liefergebiet/` 0 / 29. 12 von 17 Klicks laufen über eine Adresse, die weder kanonisch noch verschlüsselt ist | 2026-09-08 |
| EIG21 | „second hand alsfeld öffnungszeiten": Platz 1.4, 23 Impressionen, 1 Klick — keine Seite beantwortet die Frage | local-seo | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): GSC 09.08.–05.09.2026; dazu „geschäfte jetzt geöffnet" (Platz 11) und „second hand in der nähe jetzt geöffnet" (Platz 1). Dass es kein Ladengeschäft gibt, steht live nur im Fließtext (`shop1/templates/shop1/ueber_uns.html:68`) und in llms.txt — in keinem Titel und keiner Meta-Beschreibung, die im Suchergebnis erscheint | 2026-09-08 |
| EIG22 | Für Gäste endet die Produktseite in zwei englischen Systemtexten statt in einem Kaufweg | inhalte | offen | Tiefenanalyse 08.09.2026 (Schwere: wichtig): Live `/produkt/custom-print-hoodie/` (08.09.2026) zeigt abgemeldeten Besuchern als einzige Handlungsangebote „🔐 Auth Required" und „📝 Initiate Registration"; Warenkorb und Checkout sind `@login_required` (`shop1/views/cart.py`, `checkout.py:73`). Bei 486 Impressionen im Monat ist die Pflichtregistrierung die erste Abbruchstelle, und sie wird auf Englisch angekündigt | 2026-09-08 |
| EIG23 | Bei Vorab-Überweisung wird der Warenkorb geleert, bevor irgendetwas bezahlt ist | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: hinweis): shop1/views/checkout.py:132-138` leitet nach dem Versand der Bankdaten direkt auf `payment_success`, und `checkout.py:255` löscht dort `cart.items.all()`. Die Bestellung bleibt `pending`, das Stück bleibt für alle kaufbar — der Käufer hat aber weder Ware noch Warenkorb | 2026-09-08 |
| EIG24 | Zwei Doku-Punkte stehen als offen, sind live aber erledigt | aufgaben | offen | Tiefenanalyse 08.09.2026 (Schwere: hinweis): doku/30-INHALTE.md:100` und `doku/90-NOTIZEN.md:173` führen den Widerspruch „Impressum `noindex`, steht aber in der Sitemap" (SU11) als ungelöst; live enthält sitemap.xml am 08.09.2026 kein `/impressum/`, und `shop1/views/legal.py:281-285` begründet den Ausschluss ausdrücklich. Ebenso ist `doku/30-INHALTE.md:73` („live steht auf `/kontakt/` Operationell: 24/7") überholt | 2026-09-08 |
| EIG25 | Die Sitemap bricht mit `KeyError`, sobald eine Seite ohne Registereintrag ergänzt wird | technik | offen | Tiefenanalyse 08.09.2026 (Schwere: hinweis): shop1/views/legal.py:291` greift ungeschützt mit `SEITEN_STAND[page["name"]]` zu, während `shop1/seiten_stand.py:74` für denselben Zweck bewusst `None` zurückgibt. Heute passt beides zusammen; ein vergessener Eintrag nimmt nicht eine Zeile, sondern die ganze sitemap.xml aus dem Betrieb | 2026-09-08 |
| EIG26 | zaehle()` erneuert die Ablaufzeit bei jedem Treffer — kein Zeitfenster ist eins | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG27 | /ueber-mich/` steht in keiner Navigation und in keiner Fußzeile | konversion | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG28 | Der Kopf von `00-STATUS.md` nennt 84,4, der Messblock darunter 93,1 | status | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG29 | Fünf Punkte stehen als „offen" oder „fehlt", sind live erledigt | aufgaben | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG30 | Die Tabelle „Zahlen, die in mehreren Quellen unterschiedlich stehen" nennt selbst drei überholte Werte | notizen | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG31 | theme-color` ist zwei Stufen heller als der Seitengrund | design | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG32 | /favicon.ico` und `/apple-touch-icon.png` lösen statische Pfade in Python auf, die keine Prüfung abdeckt | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG33 | Der Messbefund PJ10 „zwei Views ohne Route" trifft nicht zu | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG34 | anfrage_verarbeiten` sagt im Vertrag „False bei Fehler" und gibt immer True | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG35 | priceRange` im Schema nennt 500 € als Untergrenze, die es einzeln nicht gibt | seo | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG36 | Die Datenschutzerklärung sagt „kein weiterer Anbieter beteiligt", während jede Formularnachricht über Webador läuft | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG37 | „100 % Echte Aufträge" und „5★ Kundenzufriedenheit" stehen unbelegt auf der Galerieseite | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG38 | Die Kundenliste nennt als wichtigsten Punkt ein Passwort, das seit dem 06.09. gesetzt ist | aufgaben | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG39 | Auf `/ueber-uns/` ist der auffälligste Knopf ein Verweis auf die Website des Entwicklers | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG40 | Die Datenschutzerklärung datiert sich mit `{% now %}` selbst und ist damit immer „aktuell" | technik | offen | bei einer anderen betreuten Seite aufgefallen am 08.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-08 |
| EIG41 | Der Festpreis der Website und die AGB widersprechen sich | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG42 | CLAUDE.md nennt durchgehend eine überholte Testzahl | wegweiser | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG43 | Die drei Pakete vom 08.09. liegen nur lokal — `main` kennt sie nicht, die Doku führt sie als erledigt | aufgaben | offen | Tiefenanalyse 10.09.2026 (Schwere: kritisch): git branch --contains ecc84f2` (TS11) / `e36f4bc` (PJ05) / `a1169a4` (SI08) nennt allein `sofort/2026-09-08-pj05-und-2-weitere`, kein `origin`-Zweig, kein `main`. Auf `main` steht `mainweb/settings.py:45` weiter `os.getenv('CANONICAL_HOST', '')` (leer) und `:335` `CSP_MODUS` Vorgabe `report-only`. `doku/80-AUFGABEN.md:151` führt alle drei als „Erledigt 08.09.2026", `:186` bewertet TS11 als „beim Kunden — die 301 bleibt gebaut". Der Messlauf vom 10.09. meldet beide Punkte unverändert rot | 2026-09-10 |
| EIG44 | Jede Mail des Shops geht von einer Domain aus, die es nicht gibt | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: kritisch): mainweb/settings.py:274` `DEFAULT_FROM_EMAIL = os.getenv('DEFAULT_FROM_EMAIL', 'noreply@luviq-shop.de')`; `shop1/utils.py:20` nimmt ihn als Brevo-Absender, `shop1/views/shop.py:83` als **Empfänger**-Rückfall der Kontaktanfragen (`os.getenv('ADMIN_EMAIL', settings.DEFAULT_FROM_EMAIL)`). DNS-Abfrage 10.09.2026 (dns.google): `luviq-shop.de` A und MX → Status 3 (NXDOMAIN), die Domain existiert nicht. MW01/MW04 messen `luviq-alsfeld.com` — versendet wird unter einer dritten Domain, für die sich SPF/DKIM gar nicht setzen lassen | 2026-09-10 |
| EIG45 | Ein Kunde, der sein Konto löscht, löscht alle seine Bestellungen mit | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: kritisch): shop1/models.py:219` `Order.user = ForeignKey(User, on_delete=models.CASCADE)`; `shop1/views/auth.py:112` `user.delete()` in `delete_account`. Damit verschwinden auch bezahlte Bestellungen samt `OrderItem` (`models.py:252`, ebenfalls CASCADE) aus der Datenbank — es gibt weder Anonymisierung noch Export. Aufbewahrungsfristen für Belege (§ 257 HGB, § 147 AO) laufen dagegen weiter | 2026-09-10 |
| EIG46 | Der Warenkorb fasst zwei verschiedene Stücke zusammen, weil er nur den Namen kennt | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/views/cart.py:21` `cart.items.get_or_create(produkt_name=produkt.name, …)`. Live tragen `/produkt/custom-print-hoodie-1/` (59,00 €) und `/produkt/custom-print-hoodie/` (58,96 €) denselben Namen „Custom print hoodie" (llms.txt, abgerufen 10.09.2026). Wer beide anklickt, bekommt einen Posten mit Menge 2 zum Preis des zuerst gelegten; das zweite Unikat ist über den Warenkorb nicht erreichbar | 2026-09-10 |
| EIG47 | Ein abgeschaltetes Stück lässt sich weiterhin in den Warenkorb legen | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/views/cart.py:14-16`: `get_object_or_404(Produkt, id=produkt_id)` ohne `aktiv=True`, geprüft wird allein `lagerbestand < 1`. Die Detailseite filtert dagegen auf `aktiv=True` (`shop1/views/shop.py::produkt_detail_slug`). Ein nach dem Verkauf deaktiviertes Stück bleibt über `/add-to-cart/<id>/` bestellbar, und `checkout()` (`shop1/views/checkout.py:73-140`) prüft weder `aktiv` noch Bestand erneut | 2026-09-10 |
| EIG48 | Sitemap und `dateModified` melden für neun Seiten den 01.09., geändert wurden sie am 08.09. | seo | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/seiten_stand.py` führt `home`, `produkte`, `kontakt`, `gaestebuch`, `ueber_uns`, `liefergebiet`, `datenschutz`, `agb`, `wissen` auf `'2026-09-01'`; auf `main` datieren `kontakt.html` und `produkte.html` auf `b35f6e4` (08.09.), `agb.html` auf `2b26108` (08.09.). Live-Sitemap vom 10.09.2026 nennt für alle neun `<lastmod>2026-09-01`. TS16 gilt als bestanden — die Angabe ist trotzdem falsch | 2026-09-10 |
| EIG49 | Nirgends steht, ob Versandkosten anfallen | recht | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): Suche über alle Templates auf `main` nach „Versandkost", „zzgl.", „inkl." → ein einziger Treffer, und der ist ein Entwicklerkommentar (`shop1/templates/shop1/wissen/bestellen.html:28`: „Keine Versandkosten, keine Bearbeitungsdauer, keine Zahl, die nirgends steht"). `shop1/views/checkout.py:83` bildet den Gesamtbetrag allein aus Positionen minus Rabatt. Weder Produktseite noch AGB noch Checkout sagen, ob Porto dazukommt (§ 6 PAngV) | 2026-09-10 |
| EIG50 | Das Impressum stützt sich auf zwei Rechtsgrundlagen, die es nicht mehr gibt | recht | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/templates/shop1/legal/impressum.html:63` überschreibt die Pflichtangaben mit „Angaben gemäß § 5 TMG" — das TMG ist seit 14.05.2024 durch das DDG ersetzt (§ 5 DDG). `:106` verlinkt `https://ec.europa.eu/consumers/odr/`; die EU hat die OS-Plattform zum 20.07.2025 eingestellt, seither führt die Verlinkung ins Leere. Live am 10.09.2026 beides so ausgeliefert. Das ist zugleich der offene Fremdpunkt EIG05, hier belegt | 2026-09-10 |
| EIG51 | Das Besuchsprotokoll speichert volle IP-Adressen ohne Frist und ohne Löschroutine | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/middleware.py:283-300` legt je Pfad und Fünf-Minuten-Fenster einen `VisitorLog` mit ungekürzter IP an (`_get_ip`, `:311-315`). `shop1/templates/shop1/legal/datenschutz.html:41` nennt Zweck und Rechtsgrundlage, aber **keine Speicherdauer** (Art. 13 Abs. 2 lit. a DSGVO); im Projekt gibt es keinen Befehl und keine Aufgabe, die alte Einträge löscht (zwei Management-Befehle insgesamt: `fix_pystore_schema`, `pruefe_seite`) | 2026-09-10 |
| EIG52 | Der Ratgeber verspricht, ein verkauftes Stück verschwinde aus dem Shop — bei Vorab-Überweisung passiert das nie | inhalte | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/templates/shop1/wissen/bestellen.html:87` und `:176` (live indexiert): „das gekaufte Stück wird im Shop abgeschaltet – ein verkauftes Unikat kommt nicht zurück in die Übersicht". `shop1/views/checkout.py:132-138` (Zweig `bank_transfer`) setzt weder `aktiv` noch `lagerbestand`; das geschieht erst, wenn jemand im Panel von Hand auf „Bezahlt" stellt (`shop1/admin_views.py:745-750`). `doku/90-NOTIZEN.md:191` hält das Gegenteil des Beitrags fest | 2026-09-10 |
| EIG53 | Die eingebettete Karte bewirbt die Privatanschrift als Standort, den es zum Besuchen nicht gibt | local-seo | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/templates/shop1/_reviews_map.html:13-19` (Startseite) und `:97-103` (Gästebuch) betten `maps.google.com?q=Grünberger+Str.+16,+36304+Alsfeld` ein, `:125-129` bietet „Auf Google Maps ansehen". Zugleich sagen `llms.txt` („Einen Laden zum Reinschauen gibt es nicht") und `shop1/templates/shop1/ueber_uns.html` dasselbe. Die Suchanfragen laufen laut GSC-Auswertung genau darauf zu („second hand alsfeld öffnungszeiten", EIG21) | 2026-09-10 |
| EIG54 | Die Erfolgsseite der Zahlung prüft den Status nicht und räumt den Warenkorb | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/views/checkout.py:246-256`: `payment_success` lädt die Bestellung allein über `id` und `user`, löscht `cart.items.all()` und rendert; `shop1/templates/shop1/payment_success.html:41` zeigt für alles außer `bank_transfer` „Transfer erfolgreich verarbeitet." Wer die PayPal-Zahlung abbricht und `/payment/success/<id>/` aufruft, sieht bei `status='pending'` eine Zahlungsbestätigung und steht ohne Warenkorb da | 2026-09-10 |
| EIG55 | Fremder Text erscheint ohne Freigabeschritt auf der Startseite | inhalte | offen | Tiefenanalyse 10.09.2026 (Schwere: wichtig): shop1/views/gaestebuch.py:24-51`: `comment_add` legt den Beitrag sofort öffentlich an, es gibt kein Freigabe- oder Meldefeld im Modell (`shop1/models.py:419-423`); `shop1/views/shop.py:32-38` zieht die vier neuesten in die Startseite, `_reviews_map.html:46-59` stellt sie mit Namen, Datum und fünf Sternen dar. Voraussetzung ist allein ein Konto — und die E-Mail-Bestätigung wird nirgends erzwungen | 2026-09-10 |
| EIG56 | Die E-Mail-Bestätigung ist folgenlos, ihre Erfolgsmeldung ungeprüft | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: hinweis): shop1/views/auth.py:52-58`: das `try` umschließt nur `messages.success('Eine Bestätigungs-E-Mail wurde an … gesendet')`, nicht den Versand (der läuft über `signals.py` und `utils.py:62` in einem Thread). `auth.py:24-27` lässt unbestätigte Konten anmelden, `views/cart.py` und `checkout.py:73` verlangen nur `login_required` — `email_verified` wird an keiner Stelle geprüft, die einen Kauf verhindert | 2026-09-10 |
| EIG57 | Jedes verkaufte Unikat nimmt seine indexierte Seite mit in den 404 | seo | offen | Tiefenanalyse 10.09.2026 (Schwere: hinweis): shop1/views/shop.py::produkt_detail_slug` — `get_object_or_404(Produkt, slug=slug, aktiv=True)`; `checkout.py:226-232` setzt nach der Zahlung `aktiv = False`. Es gibt keinen „verkauft"-Zustand, keine 301 auf `/produkte/`, kein 410. Das Silo `/produkt/` stellt 5 von 17 Live-Adressen (SU10) — bei planmäßigem Verkauf schrumpft der indexierbare Bestand mit jeder Bestellung | 2026-09-10 |
| EIG58 | Werbe-Impressionen zählen jeden Startseitenaufruf, auch den der Crawler | technik | offen | Tiefenanalyse 10.09.2026 (Schwere: hinweis): shop1/views/shop.py:22-30` erhöht bei **jedem** Aufruf von `startseite()` `impressionen` und den `WerbungStat`-Tageswert — ohne Bot-Erkennung und ohne Sitzungs-Dedup (der Dedup in `middleware.py:270-280` gilt nur für `VisitorLog`). `shop1/views/legal.py:35-49` lässt 13 KI-Crawler ausdrücklich zu. Die Zahlen im Panel `/shop-admin/werbung/` und in der geteilten `pystore`-Datenbank sind dadurch nicht belastbar | 2026-09-10 |
| EIG59 | Das „Unternehmen“-Menü der Kopfnavigation ist mit der Tastatur nicht erreichbar | design | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG60 | security.txt rechnet sein Ablaufdatum bei jedem Abruf neu und kann deshalb nie ablaufen | technik | offen | bei einer anderen betreuten Seite aufgefallen am 10.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-10 |
| EIG61 | Die Doku führt GE32 und MW15 als „nicht auf main, nicht ausgeliefert“, beides ist gemergt und live | status | offen | bei einer anderen betreuten Seite aufgefallen am 11.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-11 |
| EIG62 | Der CI-Lauf behauptet, nichts auszulassen, fährt die Testsuite aber nicht und nutzt eine andere Python-Fassung als die lokalen Läufe | technik | offen | bei einer anderen betreuten Seite aufgefallen am 11.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-11 |
| EIG63 | PayPal bucht das Geld im Browser ab, bevor der Server die Zahlung prüft | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: kritisch): shop1/templates/shop1/payment.html:144-166`: `actions.order.capture()` läuft zuerst, danach erst `/paypal/capture/`. Scheitert die Prüfung in `checkout.py:212-213` (fehlendes `PAYPAL_SECRET` → `checkout.py:36-38`, `PAYPAL_MODE` ohne Wert ergibt Sandbox-API `:23`, Zeitlimit 10 s), ist das Geld gebucht, die Bestellung bleibt `pending`, und die Kundin liest „❌ Zahlung fehlgeschlagen … Im Sandbox-Modus funktionieren keine echten Karten!“. `pruefe_seite.py:190-205` prüft keine `PAYPAL_*`-Variable | 2026-09-11 |
| EIG64 | Die AGB nennen einen Bestellknopf, den es im Shop nicht gibt | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: kritisch): shop1/templates/shop1/legal/agb.html:42`: „Durch den Klick auf "Zahlungspflichtig bestellen" gibst du ein verbindliches Angebot ab“. Der einzige Knopf, der die Bestellung anlegt (bei Überweisung auch die Bankdaten verschickt), heißt „Continue to Payment →“ (`checkout.html:113-115`). „Zahlungspflichtig“ steht in keiner Vorlage außer den AGB, und Checkout, Zahlung und Warenkorb verlinken weder AGB noch Widerruf (§ 312j Abs. 3 BGB) | 2026-09-11 |
| EIG65 | Registrierung und Checkout verschicken Mails mit ungeprüftem HTML an beliebige Adressen | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/signals.py:56` setzt `first_name`, `checkout.py:276,283` setzt `vorname`/`adresse`, `checkout.py:322` setzt `vorname` jeweils per f-String unmaskiert in `htmlContent`. Die Empfängeradresse ist frei wählbar (`wissen/bestellen.html:153`: „muss nicht die des Kontos sein“). Wer ein Konto hat, kann so Mails vom Brevo-Absender des Shops mit eigenem Link und Luisa Brehlers Bankdaten an Dritte schicken lassen. Dasselbe gilt für die Kontaktmail an die Betreiberin (`views/shop.py:88-91`) | 2026-09-11 |
| EIG66 | Wer per Überweisung zahlt, bekommt die zugesagte Bestätigung nie | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): checkout.py:339` (Bankdaten-Mail): „Sobald die Zahlung eingegangen ist, erhältst du eine Bestätigung.“ Ebenso `wissen/bestellen.html:111,194` (indexiert): „die Bestätigung kommt erst danach“. Beim Setzen auf „bezahlt“ im Panel (`admin_views.py:739-754`) wird aber keine Mail verschickt; `send_order_confirmation_email` wird nur in `paypal_capture` aufgerufen (`checkout.py:235`) | 2026-09-11 |
| EIG67 | „Keine Drucke“, aber alle Stücke heißen „print“ | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/templates/shop1/index.html:315`: „Es gibt keine Drucke und keine Serie“; `ueber_uns.html:108`: „Keine Drucke, keine Massenware“. Live heißen die Stücke „Custom print hoodie“, „Custom print jacke“ usw., mit Beschreibungen wie „Hoodie mit backprint“ und „Pants with custom print“ (llms.txt). `/produkt/custom-pants-sold/` zeigt am 11.09.: „Custom Pants (Sold) ist ein handbemaltes 1-of-1 Unikat … Pants with custom print“ | 2026-09-11 |
| EIG68 | Keine Seite außerhalb des Wissensbereichs verlinkt ihn, zwei Texte verweisen ohne Link darauf | seo | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): Das erklärt TS23/IS27: `templates/base.html:300-304` (Menü) und `:414-417` (Fuß) haben keinen `/wissen/`-Link, `{% url 'wissen' %}` kommt nur in `wissen/*.html` vor. `seiten_stand.py:45`: „Die Wissensseiten stehen in keinem Menü“. `kontakt.html:81` und `produkte.html:65` schreiben „…stehen ausführlich im Wissensbereich“ als reinen Text, weil die Designwache keine neuen Elemente zulässt. Der Docstring `views/wissen.py:21-22` behauptet dagegen „erreichbar, verlinkt“ | 2026-09-11 |
| EIG69 | Das Hero-Skript der Startseite steht zweimal im HTML | performance | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/templates/shop1/index.html:535-695`: `{% block extra_js %}` steckt in `{% block content %}` (`:115-729`), `base.html` rendert `extra_js` zusätzlich in Zeile 502. Django gibt den Block deshalb an beiden Stellen aus: GSAP-Tag, Hero-Animation und das Nachladen von Three.js (`:680-691`) laufen doppelt. Dazu passt die Messung PF21 mit „36 eingebundenen Skripten“ = 17 Seiten × 2 Alpine-Skripte + 2× GSAP | 2026-09-11 |
| EIG70 | Nach dem Login landet jede Kundin auf der Startseite statt beim Stück | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/views/auth.py:34` leitet immer auf `redirect('home')` um, ein `next` wird weder im View noch in `login.html` gelesen. `@login_required` (`cart.py:11`, `checkout.py:72`) hängt `?next=` an, das dann verloren geht, und der Gast-Knopf auf der Produktseite (`produkt_detail.html:158`) übergibt gar keins. Wer kaufen will, muss das Stück nach der Anmeldung neu suchen | 2026-09-11 |
| EIG71 | Der Vermerk `offen-ok` an `register()` behauptet eine Begrenzung, die es nicht gibt | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/views/auth.py:47-49`: „django-axes begrenzt die Versuche“. Axes zählt nur fehlgeschlagene Logins (`settings.py:138-143`), nicht Registrierungen. Jede Registrierung löst sofort eine Mail aus (`signals.py:21-22`), ohne Zahlgrenze. PJ05 wurde mit dieser Begründung als erledigt geführt | 2026-09-11 |
| EIG72 | Die Axes-Sperre gilt hinter Railways Proxy wohl pro Benutzername statt pro Person | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): settings.py:143` sperrt nach `['username','ip_address']`. `requirements.lock` enthält kein `django-ipware`, also nimmt axes 8.3.1 `REMOTE_ADDR`, und das ist hinter dem Railway-Proxy dessen Adresse. Folge: Zehn Fehlversuche von beliebiger Stelle sperren ein Konto eine Stunde lang für alle. Benutzernamen stehen öffentlich im Gästebuch. Live nicht nachgeprüft | 2026-09-11 |
| EIG73 | Die Doku führt IS18 als nicht gemergt, er ist aber live | status | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): doku/00-STATUS.md:7,31,99` und `doku/80-AUFGABEN.md:41` („Offen Nr. 1: … mergen“) sowie `30-INHALTE.md:118` sagen „nicht gemergt“. Git hat den Merge `1a5f36b`. Live-Sitemap am 11.09.: `/`, `/produkte/`, `/wissen/` mit lastmod `2026-09-11`; `/produkte/` enthält den IS18-Text zu Eigentumsvorbehalt und Reservierung | 2026-09-11 |
| EIG74 | Die Datenschutzerklärung nennt weder Beschwerderecht noch Widerspruchsrecht | inhalte | offen | Tiefenanalyse 11.09.2026 (Schwere: wichtig): shop1/templates/shop1/legal/datenschutz.html:67-75` („4. Ihre Rechte“) nennt nur Auskunft, Berichtigung und Löschung. „Beschwerde“, „Aufsichtsbehörde“ und „Widerspruch“ kommen in keiner Vorlage vor, obwohl `:41` das Besuchsprotokoll auf Art. 6 Abs. 1 lit. f stützt (Art. 13 Abs. 2 lit. b/d DSGVO) | 2026-09-11 |
| EIG75 | Zu lange Eingaben im Checkout enden auf PostgreSQL in einer Fehlermeldung ohne Grund | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): shop1/views/checkout.py:99,107-120` übernimmt `telefon`/`postleitzahl` ohne Längenprüfung in `Order` (`models.py:230` `max_length=10`, `:232` `max_length=20`). `checkout.html:61` hat kein `maxlength`. PostgreSQL wirft `DataError`, `checkout.py:142-144` fängt ihn ohne Protokoll ab: „Ein Fehler ist aufgetreten“. Die Tests laufen auf SQLite, das die Länge nicht prüft | 2026-09-11 |
| EIG76 | Ein Produktname mit „/“ lässt den Warenkorb mit 500 abstürzen | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): shop1/urls.py:25-26` nutzt `<str:produkt_name>`, und `str` passt nicht auf „/“. `warenkorb.html:55,68` rufen `{% url 'update_cart' item.name %}` auf, das ergibt `NoReverseMatch`. Der Name ist im Panel frei wählbar (`forms.py:208`), etwa „Hoodie 1/1“ – dann ist der Warenkorb nicht mehr erreichbar | 2026-09-11 |
| EIG77 | Der Menüpunkt „Django Core Admin“ führt ins 404 | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): templates/base.html:331` verlinkt fest `/admin/`, während `mainweb/urls.py:26` den Admin unter `ADMIN_URL` einhängt. Die Messung (SI14) bestätigt live: `/admin/` = 404. Damit ist auch der in PJ05 genannte Weg zur DSGVO-Auskunft über den `Subscriber`-Admin aus dem Menü nicht erreichbar | 2026-09-11 |
| EIG78 | Das Liefergebiet behauptet eine Kundenverteilung, die nirgends belegt ist | local-seo | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): shop1/views/shop.py:107-111` ist eine feste Liste, keine Bestelldaten. `liefergebiet.html:94` nennt sie „Ein Auszug aus unserer Community: 16 Orte entlang der Strecke Fulda – Alsfeld – Gießen“ (darunter Homberg (Efze), Schwalmstadt), `:53,124` (auch im FAQPage-Schema): „Ein Großteil unserer Community kommt aus dem Vogelsbergkreis“. Die Ortslisten weichen voneinander ab: `index.html:357-366` (mit Wiesbaden, Kassel), `base.html:81-103` (mit Frankfurt, Marburg, Kassel) | 2026-09-11 |
| EIG79 | Schema und llms.txt versprechen Shirts, im Bestand gibt es keine | seo | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): templates/base.html:113` hat den Katalogeintrag „Handbemalte Vintage-Shirts“, `shop1/views/legal.py:224-225` schreibt „vor allem Jacken und Shirts“. Live im Bestand am 11.09.: drei Hoodies, eine Hose, eine Jacke (`/produkte/`) | 2026-09-11 |
| EIG80 | Die Kontaktmail hat keine Antwortadresse | technik | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): shop1/utils.py:30-35` baut die Brevo-Anfrage ohne `replyTo`, `views/shop.py:89-91` schickt die Anfrage mit dem Shop-Absender. Wer im Postfach auf „Antworten“ klickt, schreibt an den Absender (Vorgabe `noreply@luviq-shop.de`, laut EIG44 eine nicht existierende Domain), nicht an die Kundin | 2026-09-11 |
| EIG81 | PJ09 empfiehlt, sieben Vorlagen zu löschen, die alle ausgeliefert werden | aufgaben | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): Die Messung nennt `lockout.html` und sechs `wissen/*.html` als verwaist. Tatsächlich rendert `shop1/views/wissen.py:149` sie über `render(request, beitrag['template'])`, und `settings.py:140` bindet `lockout.html` ein. Darunter sind drei indexierte Seiten mit über 1.000 Wörtern. `doku/90-NOTIZEN.md:176` kennt den Fehlalarm nur für vier Vorlagen | 2026-09-11 |
| EIG82 | Die Aufgabenliste nennt das Paket-Tor leer, dabei läuft die Testsuite dort und in der CI | aufgaben | offen | Tiefenanalyse 11.09.2026 (Schwere: hinweis): doku/80-AUFGABEN.md:55`: „Die Liste ist leer … 0 von 0 grün“. `pystore-overview/sites.json:283-285` hat für luviq `"python manage.py test"`, `.github/workflows/pruefungen.yml:94-95` fährt `manage.py test`. EIG62 trifft hier nur halb zu: Die Tests laufen, aber die CI nutzt Python 3.12 (`:73`) statt 3.11 wie im Container | 2026-09-11 |
| EIG83 | Der KV07-Zweig steht in der Doku als „nicht gemergt“ und ist seit dem Merge live | status | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): git log` auf `main`: `c522ff9 Merge sofort/2026-09-11-kv07-und-2-weitere`; `doku/00-STATUS.md:7,44,49,99` und `doku/80-AUFGABEN.md:41` („Offen Nr. 1: Zweig … mergen und pushen“) sagen das Gegenteil. Live am 12.09.2026: `https://www.luviq-alsfeld.com/kontakt/danke/` antwortet 200, h1 „Danke für deine Nachricht“ | 2026-09-12 |
| EIG84 | Die Produktseite kennt keinen Ausverkauft-Zustand — der Kaufknopf endet auf der Startseite | inhalte | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): produkt_detail.html:121` zeigt fest „Einzelstück · 1 of 1“ in Grün, `:151-156` zeigen immer „🛒 Add to Orbit“ und „💳 Deploy Now“, ohne `lagerbestand` zu prüfen. `cart.py:16-18` weist den Klick dann ab und leitet auf `home` — der Besucher landet ohne Ware auf der Startseite | 2026-09-12 |
| EIG85 | Warenkorb-Posten und Gästebuch-Likes ändern sich per GET — die Doku kennt nur vier solche Stellen | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): warenkorb.html:68` ist ein `<a href="{% url 'remove_from_cart' item.name %}">`, `cart.py:46-51` löscht ohne Methodenprüfung; `gaestebuch.html:88` ist ein `<a>` auf `comment_like`, `gaestebuch.py:56-63` toggelt. `CLAUDE.md` und `doku/80-AUFGABEN.md` (Beim Kunden Nr. 10) nennen nur `comment_delete`, `admin_produkt_toggle`, `admin_resend_newsletter`, `admin_newsletter_reset | 2026-09-12 |
| EIG86 | Jeder anonyme Besucher bekommt ein 14-Tage-Sitzungs-Cookie allein für die Reichweitenmessung | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): middleware.py:283-290` schreibt `last_visit_date`, `:296-332` `visited_paths` in die Session — bei der ersten Antwort, ohne Konto und ohne Warenkorb; `settings.py:289` `SESSION_COOKIE_AGE = 1209600`. `datenschutz.html:62` nennt als Zweck nur „Ihren Warenkorb und Ihren Login-Status … Ohne diese ‚Anker‘ könnte der Shop nicht funktionieren“ | 2026-09-12 |
| EIG87 | „Jetzt bei Google bewerten“ führt auf dieselbe Maps-Suche wie der Knopf darunter | local-seo | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): reviews_map.html:120` verlinkt `{{ GOOGLE_REVIEW_URL }}`, Vorgabe in `settings.py:409`: `https://www.google.com/maps/search/Luviq+Universe+Alsfeld`. `_reviews_map.html:125` verlinkt wörtlich dieselbe Adresse als „Auf Google Maps ansehen →“. Ohne gesetzte Umgebungsvariable sind es zwei Beschriftungen, ein Ziel — und nie ein Bewertungsformular | 2026-09-12 |
| EIG88 | Der gewährte Rabatt wird in die Sitzung geschrieben und nirgends gelesen | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): checkout.py:122` `request.session['order_rabatt'] = …`; die Suche über das ganze Projekt findet keine zweite Fundstelle (nur diese Zeile). `Order` hat kein Rabattfeld (`models.py:201-247`) — der Grund für einen um 10 % geminderten Gesamtbetrag steht danach weder in der Bestellung noch im Panel noch in einer Mail | 2026-09-12 |
| EIG89 | Das Newsletter-Formular hat weder `method` noch `action` — ohne JavaScript geht die Adresse in die Adresszeile | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): index.html:525` `<form id="newsletter-form" class="…">`, abgeschickt wird nur über den Skript-Zuhörer `:698-727`. Bricht das Skript (CSP, Blocker, Ladefehler), sendet der Browser ein GET auf `/`, die Startseite lädt neu als `/?email=…`, es passiert nichts und es steht nichts da. Das ist zugleich der Messbefund VL13 („ohne POST: /“) | 2026-09-12 |
| EIG90 | Das Kontaktformular verliert alles Getippte, sobald die Serverprüfung greift | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: wichtig): shop.py:84` prüft die Felder nach `.strip()`, `:101-104` rendert `kontakt.html` **ohne Kontext**; die Felder `kontakt.html:94-109` tragen kein `value`/keinen Inhalt. Ein Betreff aus einem Leerzeichen kommt an `required` vorbei, läuft in `messages.error` und löscht die ganze Nachricht | 2026-09-12 |
| EIG91 | Das Knappheitsabzeichen „Limited“ trägt jede einzelne Karte | inhalte | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): produkte.html:92` `{% if produkt.lagerbestand < 3 %}` → „Limited“. `models.py:95` `lagerbestand = PositiveIntegerField(default=1)`, und der Shop verkauft nach eigener Aussage ausschließlich 1-of-1-Stücke. Ein Signal, das auf allem steht, unterscheidet nichts — daneben steht ohnehin schon „Unique“ (`:91`) | 2026-09-12 |
| EIG92 | sync_session_to_db` läuft bei jeder Anmeldung und tut nichts | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): helpers.py:24-46` liest `request.session['warenkorb']` und leert ihn; die einzigen weiteren Fundstellen im Projekt sind die Leerung `:45` und `tests/test_warenkorb.py:143`. Keine View schreibt diesen Schlüssel — der Warenkorb liegt seit dem Datenbank-Umbau in `Cart`/`CartItem`. Die tote Funktion trägt einen `offen-ok`-Vermerk aus PJ05 und einen Test | 2026-09-12 |
| EIG93 | Der Ebenensprung h1→h3 auf zehn Seiten hat eine einzige Ursache: es gibt gar keine `h2 | seo | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): agb.html:36,41,46,51,56` und `datenschutz.html:36,46,56,68` setzen alle Abschnittsüberschriften als `h3`, ohne dass je eine `h2` davorsteht (Messung IS14, BF15, VL17). Die Schriftgröße steckt in den Klassen (`text-xs font-black uppercase`), nicht in der Stufe — eine Umstellung auf `h2` ändert nichts am Aussehen, nur die Designwache-Referenz ist nachzuziehen | 2026-09-12 |
| EIG94 | Die AGB sagen, wann der Kunde bestellt — nicht, wann Luviq annimmt | inhalte | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): agb.html:42` (§ 2): „Durch den Klick … gibst du ein verbindliches Angebot ab“, davor „Die Darstellung … stellt kein rechtlich bindendes Angebot dar“. In keinem der fünf Paragraphen steht, wodurch und wann der Vertrag zustande kommt; § 4 (`:52`) regelt nur, dass nach Zahlungseingang versendet wird. Nach dem Lesen ist die Frage „habe ich das Stück jetzt?“ unbeantwortet | 2026-09-12 |
| EIG95 | Die volle Besucher-IP geht unverschlüsselt über HTTP an einen Drittdienst | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): middleware.py:222`: `url = f'http://ip-api.com/json/{ip}?fields=…'`. Die Datenschutzerklärung benennt das offen (`datenschutz.html:41`: „die Übertragung erfolgt derzeit unverschlüsselt über HTTP“) — in `doku/80-AUFGABEN.md` steht der Punkt weder unter „Offen“ noch unter „Fehlt“, er ist also erklärt, aber nicht eingeplant | 2026-09-12 |
| EIG96 | Fehlt das Produkt in der Datenbank, nimmt der Warenkorb jede Menge an | technik | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): cart.py:55-75`: die Obergrenze kommt aus `Produkt.objects.filter(name=produkt_name).first()`; ist das Stück umbenannt oder gelöscht, bleibt `db_produkt` `None` und `item.menge = menge` wird ungeprüft gesetzt. `checkout.py:81` bildet daraus den Gesamtbetrag, ohne Bestand oder `aktiv` erneut zu prüfen. Die Route ist zudem nicht auf POST beschränkt | 2026-09-12 |
| EIG97 | Zwei Mails zu derselben Bestellung unterschreiben mit zwei verschiedenen Absendern | inhalte | offen | Tiefenanalyse 12.09.2026 (Schwere: hinweis): checkout.py:285` (Bestellbestätigung): „Mit freundlichen Grüßen,<br>Dein Shop-Team“; `checkout.py:340` (Bankdaten bei Vorab-Überweisung): „Herzliche Grüße,<br><strong>Luisa Brehler</strong>“. Der Betrieb ist nach Impressum und `llms.txt` eine Person | 2026-09-12 |
| EIG98 | „Lädt in unter zwei Sekunden, gemessen statt behauptet" nennt keine Messung | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG99 | Impressum und Datenschutz stützen sich auf vier aufgehobene Gesetze | recht | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG100 | Die Doku führt als „nicht ausgeliefert", was live steht | status | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG101 | Der `speakable`-Selektor markiert auf elf Seiten jeden Absatz im Inhalt | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG102 | „Langjährige Erfahrung" steht live neben dem Gründungsjahr 2025 | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG103 | Zwei Galeriefotos tragen dieselbe Beschriftung und damit denselben Sitemap-Bildtitel | seo | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG104 | Datenschutzerklärung bestreitet eine Messung, die der Code durchführt | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG105 | Besuchsstatistik zeichnet die Proxy-Adresse auf, nicht die des Besuchers | technik | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG106 | Datenschutzerklärung nennt die Datenbank nicht, in der die Anfragen liegen | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
| EIG107 | Impressum zitiert zwei aufgehobene Normen und verlinkt eine abgeschaltete Plattform | inhalte | offen | bei einer anderen betreuten Seite aufgefallen am 12.09.2026. **An dieser Seite noch nicht geprüft.** | 2026-09-12 |
<!-- eigenepunkte:ende -->
