# Festgenagelt (Messpunkt PJ11, 11.09.2026): jede Zeile mit ==, damit der
# naechste Deploy dasselbe baut wie der letzte. Die Unterabhaengigkeiten
# stehen in requirements.lock; die Constraint-Zeile unten zieht sie ueberall
# mit, wo diese Datei installiert wird - im Container (Dockerfile), im
# CI-Lauf (.github/workflows/pruefungen.yml) und lokal.
#
# Django 5.2.17 statt 6.0: der Dockerfile setzt python:3.11-slim, Django 6.0
# verlangt Python >= 3.12. Unter der alten Spanne (>=5.0,<6.1) installierte
# pip im Container deshalb ohnehin die neueste 5.2 - laut PyPI 5.2.17 vom
# 04.08.2026 -, im CI-Lauf (Python 3.12) dagegen 6.0.x. Mit dem Nagel bauen
# Container und CI dieselbe Fassung. 5.2.17 nennt auf PyPI Python 3.10 bis
# 3.14. Der Entwicklungsrechner (Python 3.14) hatte am 11.09.2026 noch
# Django 6.0.5 installiert; erst ein `pip install -r requirements.txt` zieht
# ihn auf denselben Stand.
#
# Alle uebrigen Fassungen sind die, mit denen die Testsuite am 11.09.2026
# lokal gruen lief; keine verlangt mehr als Python 3.10.
--constraint requirements.lock
Django==5.2.17
python-dotenv==1.2.2
pillow==11.3.0
django-axes==8.3.1
gunicorn==26.0.0
whitenoise==6.12.0
requests==2.34.2
dj-database-url==3.1.2
psycopg2-binary==2.9.12
cloudinary==1.44.2
django-cloudinary-storage==0.3.0
